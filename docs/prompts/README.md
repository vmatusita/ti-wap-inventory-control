# Ordens de serviço — como usar

Cada arquivo `F*.md` desta pasta é um prompt completo para uma fase, escrito para ser **colado inteiro no Claude Code**, aberto na raiz deste repositório.

## Fluxo de cada fase

1. Confira no `README.md` que a fase anterior está fechada (checklist de aceite passou).
2. Abra o Claude Code na raiz do repositório (`claude` no terminal). Ele lê o `CLAUDE.md` automaticamente — as regras de lá valem sempre.
3. Cole o conteúdo inteiro do `F<N>-*.md` da fase.
4. **Modo autônomo (CLAUDE.md):** ele decide sozinho (registrando em `docs/DECISOES.md`), executa de ponta a ponta — decisões, merge, deploy e produção inclusos — e se autoverifica pelo checklist da ordem. Só te procura se faltar um insumo físico seu (ex.: os CSVs reais).
5. Ao final, ele entrega o resumo com checklist marcado, decisões e pendências, e atualiza o `README.md`. Revise quando quiser — nada fica bloqueado esperando você.

## Regras de ouro

- **Uma ordem por vez.** Não cole duas fases na mesma sessão.
- Se o repositório mudou desde que o prompt foi escrito (ex.: você mexeu em algo à mão), me peça para atualizar a ordem antes de rodar — prompt desatualizado gera obra torta.
- Modo autônomo não elimina auditoria: o resumo de cada fase + `docs/DECISOES.md` + o histórico do git são o seu rastro para revisar depois, no seu tempo.
- Custo R$ 0 e dados 100% fictícios até a F4 — se qualquer rodada pedir cartão de crédito ou dado real, algo está errado: pare.

## As fases

| Ordem | Arquivo | Resultado |
|---|---|---|
| F0 | `F0-fundacao.md` | Projeto Next 16 + Supabase + login por convite + deploy |
| F1 | `F1-banco-e-seed.md` | Migrations do schema + banco populado com dados fictícios |
| F2 | `F2-operacao.md` | Ativos, ficha com linha do tempo, nova movimentação em lote, estorno |
| F3 | `F3-relatorios.md` | Relatórios por filial em tempo real + telas de administração |
| F3B | `F3B-relatorios-v2.md` | ✅ **CONCLUÍDA em 14/07/2026** — relatório no formato do e-mail (3 grupos + tabelas de saídas/entradas) + itens por quantidade + anotações (plano: `docs/PLANO-RELATORIOS-V2.md`) |
| F5A | `F5A-termos.md` | ✅ **CONCLUÍDA em 14/07/2026** — os 7 termos (5 de responsabilidade + 2 de devolução) gerados em `.docx` fiel ao original, preview no navegador, snapshot jsonb + Storage privado, status `gerado`. Migrations 0020–0021 (plano: `docs/PLANO-TERMOS.md`). Roda antes ou depois do go-live |
| F4 | `F4-importador-golive.md` | ✅ **CONCLUÍDA (go-live) em 15/07/2026** — carga inicial ÚNICA via scripts + ensaio + cutover (1.596 ativos, 3.231 movimentações). Decisões em `docs/DECISOES.md` (2026-07-15 · F4) |
| F5 | `F5-refino.md` | 🚧 Backlog de ordens curtas (alertas, e-mail, upload do PDF assinado). **Estoque mínimo por item e kits de movimentação (5.9) saíram na F12**, em 23/07/2026 |
| F6A | `F6A-correcoes-pos-golive.md` | ✅ **CONCLUÍDA em 16/07/2026** (via `F6A-ultracode.md`, orquestração multi-agente A1 ∥ A4 ∥ A5 → integração → rollout único → auditoria A2). Migrations 0027 (Total/Estoque + `retorno`) e 0028 (v_pendencias detalhe) em produção; carga go-live filtrada do relatório; pendências só p/ operador + página `/pendencias`. Decisões em `docs/DECISOES.md` (2026-07-16 · F6A). Pendência devolvida ao Johnny: 2 snapshots congelados do go-live com a carga |
| F6B | `F6B-melhorias-ux.md` | ✅ **CONCLUÍDA em 16/07/2026** (via `F6B-ultracode.md`, orquestração em 2 ondas em worktrees isolados: W1 semana+resumo ∥ W2 obs+tabela de itens ∥ W3 termo+patrimônio ∥ W4 sessões → integração + revisão adversarial → B1 loading ∥ B9 ajuda → revisão adversarial → migration 0030 → deploy único). Loading nativo (barra + skeletons), semana default dom–sáb, quebra no resumo, obs no snapshot, seção de itens no relatório, confirmar/desfazer assinatura, corrigir patrimônio, sessões 24h, `/ajuda`. Zero dependência nova. Decisões em `docs/DECISOES.md` (2026-07-16 · F6B) |
| F6C | `F6C-carga-saldos-itens.md` | 🚧 Carga única dos saldos iniciais de itens (pendência da F4) — **por último**, quando o export da planilha de gestão existir (decisão do Johnny: melhorias primeiro, cargas depois) |
| F7 | `F7-import-csv-ativos.md` → `F7-ultracode.md` | ✅ **CONCLUÍDA em 16/07/2026** (via `F7-ultracode.md`: W1 motor ∥ W2 banco → W3 tela → revisão adversarial de 7 dimensões, TOCTOU corrigido) e **em produção — gate resolvido, confirmado em 17/07/2026** (`import_logs` + RPC `importar_ativos_substituir` no ar; o gate era o classificador do modo automático, que barra a *execução* do import destrutivo). Tela `admin/importar`, só modo Substituir tudo (go-live por filial; Atualizar adiado); **revoga a regra "não existe importação" da spec §10** (docs emendados — §10.2). Migrations 0031/0032. Decisões em `docs/DECISOES.md` (2026-07-16 · F7) |
| F7B | `F7B-correcao-erros-import.md` → `F7B-ultracode.md` | ✅ **CONCLUÍDA em 17/07/2026** (via `F7B-ultracode.md`: W1 motor ∥ W2 banco → W3 validators+actions+UI → revisão adversarial + E2E em DEV + emendas). Erros e avisos do import se corrigem **no passo Preview**, não no CSV: agrupados por valor e corrigidos **em massa** (categoria/estado/site desconhecido/data, com sugestão por Levenshtein próprio), pontuais com o contexto da linha (patrimônio/service tag/colaborador), remoção de linha, painel com **Desfazer** e reanálise automática. CSV original imutável; correções auditadas em `import_logs.correcoes` (migration 0033 — coluna + RPC com `p_correcoes`). Régua da F7 intacta (descartado bloqueia; site de outra filial conhecida só remove). Decisões em `docs/DECISOES.md` (2026-07-17 · F7B) |
| F7E | `F7E-melhorias-import.md` → `F7E-ultracode.md` | ✅ **CONCLUÍDA em 17/07/2026 · em produção** (via `F7E-ultracode.md`: W1 motor ∥ W2 banco → integração → W3 UI/actions ∥ W4 pendência fora do import → revisão adversarial de 5 dimensões **0 defeitos** + E2E em DEV). Datas de entrega `dd/MMM` (puxam o ano da inclusão, +1 na virada) datam o **ajuste**; **patrimônio vazio** importa NULO com pendência `'sem patrimônio físico'` (lista + `/pendencias`, bucket que acolhe os "não canônico" do go-live; corrige na ficha); erros do mesmo tipo num **card só** com sugestão 1-clique do hostname. Migration `0034` (patrimônio nullable + índice parcial + RPC com pendência/data-do-ajuste/agregada). Só-números seguem bloqueantes; invariantes F7/F7B intactas. `0034` aplicada em produção pelo Johnny (SQL Editor — gate destrutivo da F7/F7B), conferida antes do merge (`78452d9`), deploy READY, smoke só-leitura OK. Decisões e ata em `docs/DECISOES.md` (2026-07-17 · F7E) |
| F7F | `F7F-ultracode.md` | ✅ **CONCLUÍDA em 17/07/2026** (via `F7F-ultracode.md`: W1 motor ∥ W2 banco/actions → integração → W3 UI → W4 revisão adversarial de 5 dimensões + E2E em DEV + emendas). Facilitadores antes do próximo go-live, **sem migration** (TS + UI): erro do "Substituir tudo" traduzido por **SQLSTATE + substring** (timeout `57014`, índice `23505`, `raise` `P0001`) e logado, `bodySizeLimit` 8 MB (**sem 0035** — `SET statement_timeout` na RPC é no-op, ~1.200 ativos em ~1,0–1,4 s); patrimônio ausente **auto-preenchido pelo Hostname** (`NB-WAP0001234`→`WAP0001234`, aviso âmbar auditável, fora do fluxo de correção); "Aplicar tudo" inclui grupos **parciais** ("N aplicadas · faltam M"); tier **âmbar** (aviso ≠ erro vermelho, AA claro/escuro); `try/catch` no aplicar/analisar. Extrator do hostname só pega o token canônico delimitado (testes de injeção). Invariantes F7/F7B/F7E intactas; import sem casos novos idêntico ao F7E. `lint`+`tsc`+`test`(539)+`build` verdes. Decisões em `docs/DECISOES.md` (2026-07-17 · F7F) |
| F8 | `F8-corrigir-import-compra-abertura.md` | ✅ **CONCLUÍDA em 20/07/2026** — a compra de abertura do import volta a ser SEMPRE baseline (fora das Entradas, com/sem data); a data real segue na ficha/linha do tempo/as-of. Reverte a F7H. Migration `0036`. Decisões em `docs/DECISOES.md` (2026-07-20 · F8) |
| F9 | `F9-ultracode.md` | ✅ **CONCLUÍDA em 22/07/2026** (fase 0 `EstadoVazio` → W1 movimentações ∥ W2 compra ∥ W3 itens ∥ W4 shell ∥ W5 ficha/admin/ajuda → integração → revisão adversarial de **9 dimensões com refutação por padrão** → emendas → deploy único). Os **14 itens da Onda 1** do [`../BACKLOG-UX.md`](../BACKLOG-UX.md), **zero migration e zero dependência nova**: busca por colaborador no combobox (M2), reset do tipo explicado (M7), chips Hoje/Ontem (M10), colar do Excel com TAB/`;` (A1), duplicata no preview (A3), defaults com memória + autofoco (A5), bipagem por leitor USB documentada (A7), filtros do histórico de itens na URL (I3), lançar da linha do saldo (I6), **correção da ajuda que prometia estoque mínimo inexistente** (I5a), badge de pendências + KPIs clicáveis (T2), confirmação ao revogar senha (T4), copiar patrimônio (T6), `EstadoVazio` padronizado (T8). Revisão: 32 achados brutos → 8 confirmados por maioria de 3 céticos → todos corrigidos. `lint`+`tsc`+`test`(613)+`build` verdes. Decisões em `docs/DECISOES.md` (2026-07-22 · F9) |
| F10 | `F10-ultracode.md` | ✅ **CONCLUÍDA em 22/07/2026** (gate + contrato §1.5 fixado → onda 1 em **worktrees isolados**: W1 motor da movimentação ∥ W2 compra ∥ W3 itens ∥ W4 export → integração → onda 2: W5 UI do fluxo de movimentação, sobre o W1 → onda 3: revisão adversarial + E2E documentado + emendas → deploy único). Os **13 itens da Onda 2** do [`../BACKLOG-UX.md`](../BACKLOG-UX.md) — o fim do "um a um" —, **zero migration e zero dependência nova**: colar/bipar a lista de patrimônios no lote, com os 4 baldes e escolha manual no patrimônio duplicado (M1); teto do lote de 10 → **30**, numa constante única (M11); recentes do operador no combobox vazio (M3); sugestões de colaborador/setor (M4); **aviso âmbar de possível duplicata** — a regra §8.7 da spec, enfim implementada, sem travar o registro (M5); rascunho do lote em `sessionStorage` com Restaurar/Descartar e re-checagem da interseção (M6); termos encadeados + chips "Já registrados" no sucesso parcial (M9); service tags pareadas com a faixa da compra (A2); memória do acervo em marca/modelo/fornecedor (A4); "Comprar outro igual" + "Repetir última compra", nunca com patrimônio/ST (A6); carrinho multi-item cuja linha que falha não derruba as outras (I1); criar item inline no meio do lançamento (I2); e **export CSV** de Ativos/Pendências/Itens respeitando os filtros da tela, com cap de 5.000 e aviso ao truncar (T5). Ajuda emendada (o manual ainda prometia lote de 10) e roteiro reproduzível em [`../E2E-F10.md`](../E2E-F10.md). Decisões em `docs/DECISOES.md` (2026-07-22 · F10) |

| F11 | `F11-ultracode.md` | ✅ **CONCLUÍDA em 22/07/2026** (gate + contrato §1.5 → fase 0 `LinkAjuda` → onda 1 na **mesma árvore**, branch única `f11`, com 6 frentes de arquivos disjuntos: W1 lista de movimentações ∥ W2 paleta e atalhos ∥ W3 ordenação e paginação ∥ W4 saldos por filial ∥ W5 relatório na URL ∥ W6 a11y dos forms → integração → onda 2: revisão adversarial + emendas + relatório). Os **7 itens da Onda 3** do [`../BACKLOG-UX.md`](../BACKLOG-UX.md) — que **fecha as três ondas de UX** —, **zero migration e zero dependência nova**: a lista `/movimentacoes` que nunca existiu, com período/tipo/filial/busca na URL, paginação e marcação de estorno/estornada (M8, com a sidebar passando a apontar para a lista e `N`/header/dashboard seguindo no formulário); paleta de busca global `Ctrl+K`/`⌘K`/`/` reusando o proxy de busca da F9, lupa no cabeçalho e atalho `?` (T1); `LinkAjuda` contextual em 8 telas, fechando o backlog do atalho `?` aberto na F6B (T3); ordenação por coluna em `/ativos` (`?ord=`, whitelist + `parseOrdenacao` puro com teste, `aria-sort`) e paginação com "página X de Y", salto e 25/50/100 (`?pp=`), com API retrocompatível — `/pendencias` e o histórico de `/itens` não foram editados (T7); `/itens?visao=filiais` com uma coluna por filial e "faltam N" na célula certa, por N chamadas **fixas** à RPC existente (I4); filtros das tabelas do relatório na URL com prefixo por tabela `sd.`/`en.`/`mv.`, testes do hook de 17 → 43 casos (T10); `useTransition` + `aria-invalid`/`aria-describedby` + foco no Cancelar nos diálogos de Ativos e Administração, **sem** migração para react-hook-form (T9 — dívida K segue aberta, por decisão). Fora do escopo, por segurança: `scripts/env-guard.ts` ganhou a lista `REFS_DE_PRODUCAO` (as guardas do seed/reset comparavam ref × URL, não identidade — e o `.env.local` da máquina apontava para produção). Revisão adversarial de 8 dimensões: 28 achados brutos → **17 confirmados** por maioria de 3 céticos → todos corrigidos. `lint`+`test`(782 na base do código, +67 sobre a F10 — 788 com os testes das emendas da ajuda)+`build` verdes; `supabase/` intocada. Decisões em `docs/DECISOES.md` (2026-07-22 · F11); evidências, roteiro E2E e pendências em [`../RELATORIO-F11.md`](../RELATORIO-F11.md) |

| F12 | `F12-ultracode.md` | ✅ **CONCLUÍDA em 23/07/2026** (gate + contrato §1.5 → onda 1 na **mesma árvore**, direto na `main`: W1 motor das duas migrations ∥ W4 auditoria adversarial read-only ∥ W5 smoke logado → onda 2: W2 UI do estoque mínimo ∥ W3 kits → onda 3: W6A correções + revisão adversarial ∥ W6B build/deploy ∥ W6C documentação). Os **dois itens que o [`../BACKLOG-UX.md`](../BACKLOG-UX.md) ainda devia à F5**, com **duas migrations aditivas e zero dependência nova**: **estoque mínimo por item** (`itens.estoque_minimo`, migration **`0042`** — campo no catálogo, coluna "Mínimo" em `admin/itens`, selo âmbar "repor" nas duas visões de `/itens` e card "Itens para repor" no painel; regra `minimo > 0 && consolidado < minimo`, sempre contra o **consolidado**, `0` = sem alerta e **igual ao mínimo não repõe** — I5, F5 §5.x) e **kits de movimentação salvos** (tabela `kits_modelos` com `payload jsonb`, migration **`0043`** — CRUD em `/admin/kits` e "Aplicar kit" no passo 2, sobrescrevendo os 4 campos, com **checklist de categorias que avisa e nunca bloqueia**; kit é **cópia**, desativá-lo não altera movimentação passada; tipo incompatível **não aplica nada**, divergência deliberada do `repetirUltima` — M12, F5 §5.9). Na mesma ordem: **auditoria dos 66 commits** que foram a produção sem smoke autenticado desde `d925c47` (43 de runtime revisados diff a diff, **9 achados, todos corrigidos** — dois altos: `/pendencias` caindo por `?page` fora de faixa e a busca de `/movimentacoes` cega para patrimônio não-canônico; causa raiz de quatro deles extraída para `src/lib/url-params.ts`), **smoke logado reexecutável** (`scripts/smoke/smoke-prod.mjs` — baseline 33 OK · 0 aviso · 3 n/a · 0 falha) e revisão adversarial de 12 pontos (achado próprio: aplicar kit herdava a **data do termo** de outra movimentação). **Nesta OS a regra "um item = um branch = uma sessão" do `F5-refino.md` foi revogada por decisão do Johnny** (§2.2 da ordem). `lint`+`test`(870)+`build` verdes. **Limite registrado: o E2E visual logado NÃO foi executado** (o agente não digita senha em formulário e o bypass de autenticação foi corretamente barrado). Decisões em `docs/DECISOES.md` (2026-07-23 · F12); evidências, os 9 achados, saídas reais e a seção "o que este relatório NÃO prova" em [`../RELATORIO-F12.md`](../RELATORIO-F12.md) |

| F13 | `F13-ultracode.md` | ✅ **CONCLUÍDA em 23/07/2026** (onda 0: 4 diagnósticos read-only ∥ → onda 1: C1 convite ∥ C2 causa-raiz/busca ∥ C3 âncoras, recortes disjuntos → onda 2: C4 responsivo na árvore integrada → onda 3: revisão adversarial de 4 lentes com refutação por padrão + síntese → emendas → rollout). Quatro defeitos relatados em produção, **zero migration e zero dependência nova** — e o diagnóstico provou que **B1 e B2 eram o mesmo defeito**: um `export type { A, B }` num módulo `'use server'` (`src/lib/actions/movimentacoes.ts`) fazia o transform de Server Actions do Turbopack registrar os tipos como valor, e o módulo **inteiro** morria com `ReferenceError` na avaliação, derrubando **todas** as suas Server Actions. Via a paleta `Ctrl+K` (F11) no layout do `(app)`, isso atingia **11 dos 16** manifestos de actions — **toda escrita do app respondia 500 em produção desde 22/07 18:50 UTC**, incluindo a entrada do visualizador por senha, com build/lint/870 testes/smoke verdes (só o POST quebrava; o GET seguia 200). **Correção: alias inline** (`export type X = Y`), mais duas guardas — teste de fonte (`src/lib/use-server-exports.ts`, provado acusando a linha 35 antes da correção) e gate de build (`scripts/verificar-actions-build.mjs`). Junto: a busca da nova movimentação volta a achar (mesmo defeito, B2); o "?" da ajuda passa a posicionar na seção certa sob o `loading.tsx`, por um client component com função pura testada (B3); o responsivo (B4) perdeu a rolagem lateral das listas (`flex-1`→`grow basis-full xl:basis-0`) e do relatório (`min-w-0` no grid), com matriz rota×breakpoint (29×6, 0 estouros) e `ui/**` intocado; e um **achado de segurança fora dos quatro bugs**: a senha da tela de definir-senha podia ir para a URL num submit pré-hidratação (`method="post"` + botão desabilitado até hidratar), mais o `error.tsx` que faltava no grupo `(app)`. Revisão adversarial de 4 lentes → **PODE PUSHAR**; 2 emendas não-bloqueantes aplicadas. `lint`+`test`(**925**)+`build`+gate de build verdes. **Limite: o convite fim a fim não pôde ser exercitado (sem `service_role` em DEV)** — confirmação final é um clique do Johnny. Decisões em `docs/DECISOES.md` (2026-07-23 · F13); evidências, matriz e "o que este relatório NÃO prova" em [`../RELATORIO-F13.md`](../RELATORIO-F13.md) |

| F14 | `F14-ultracode.md` | ✅ **CONCLUÍDA em 23/07/2026** (execução num único contexto — decisão registrada —, direto na `main`: gate + varredura §0 → W1 banco → W2 motor/fluxo + W3 leituras/ficha/relatório → integração → revisão adversarial de **5 lentes com refutação por padrão** → emenda → rollout §1.4). Manutenção com fornecedor, **duas migrations aditivas (`0044`+`0045`), zero dependência nova**: **MN1** chamado do fornecedor (`movimentacoes.chamado_fornecedor`, obrigatório no `envio_manutencao` por Zod + check `NOT VALID`, baixa visibilidade — fora de `/ativos`/`/movimentacoes`/combobox/export); **MN2** estado terminal `devolvido_fornecedor` (espelho de `descartado` em inventário/KPIs/as-of/pendências; recria `status_apos_movimentacao` e `rel_estoque_asof`); **MN3/MN4** RPC atômica `devolver_ao_fornecedor` (devolução do antigo + substituto opcional num submit; colisão → rollback total), `ativos.substitui_ativo_id` (vínculo nos 2 sentidos + "Histórico do ativo substituído" somente-leitura), fluxo próprio (`/movimentacoes/devolucao-fornecedor`, lote sempre 1). Revisão: 4 lentes limpas + 1 achado real (trigger não zerava o detentor na baixa pelo caminho `ajuste→devolução`) **corrigido** e re-revisado LIMPO. `lint`+`test`(**929**)+`build` verdes; migrations aplicadas em produção (verificação pós-apply completa, acervo intocado 1593, advisors sem achado novo); **deploy READY** (`e05856e`), smoke pós-deploy read-only OK. **Limite: sem E2E autenticado** (login wall) — motor provado por roteiro SQL (7 cenários) + Vitest; fluxo real só na próxima manutenção. Decisões em `docs/DECISOES.md` (2026-07-23 · F14); evidências em [`../RELATORIO-F14.md`](../RELATORIO-F14.md) |
| F15 | `F15-ultracode.md` | ✅ **CONCLUÍDA em 23/07/2026** (fases sequenciais na mesma árvore — precedente F14 —, direto na `main`: gate + varredura §0 → W1 banco → W2 motor/forms → W3 leituras/relatório/ajuda/ficha → integração → revisão adversarial de **5 lentes com refutação por padrão** → emendas → re-revisão → rollout §1.4). Três correções do **primeiro uso real** da devolução ao fornecedor da F14, **três migrations aditivas (`0046`/`0047`/`0048`), zero dependência nova**: **C1** service tag **obrigatória** no cadastro manual (Zod+action; sem check no banco — decisão registrada, prova empírica de que um `NOT VALID` em `ativos` quebraria movimentações de legado), o import segue aceitando vazio → pendência `'sem service tag'` (aviso âmbar no preview) + "Definir service tag" na ficha (migration `0048`); **C2** o painel de sucesso da devolução **para de ser engolido pelo guard** (remove o `router.refresh()` que reativava o guard do Server Component); **C3** o substituto nasce por **`troca`** (migrations `0046` add value + `0047` usos: 4 funções por `create or replace` puro), espelho da `compra` na máquina de estados e nas Entradas, nunca contado como compra, fora do fluxo manual/kits/duplicar — **retroativo** de 2 linhas em produção. Revisão: 4 lentes limpas + 1 achado real (**2 lentes independentes**: `troca` vazava para o select manual porque `tiposDoLote` não a excluía) **corrigido** (régua centralizada em `tiposManuaisPara`, com teste) + 1 gap próprio (aviso âmbar do import) implementado; **re-revisão LIMPA**. `lint`+`test`(**947**)+`build` verdes; migrations aplicadas em produção (verificação pós-apply completa, acervo intocado 1596, advisors sem achado novo); retroativo C3 conferido (antes=depois); **deploy READY** (`c11fb66`), smoke pós-deploy read-only OK (0 erros de runtime; rotas públicas 200). **Limite: sem E2E autenticado** (login wall). Decisões em `docs/DECISOES.md` (2026-07-23 · F15); evidências em [`../RELATORIO-F15.md`](../RELATORIO-F15.md) |

| F16 | `F16-relatorio-ux.md` | ✅ **CONCLUÍDA em 23/07/2026** (branch `f16-relatorio-ux`, commit por tarefa com gates verdes → revisão adversarial de 5 lentes → correção → merge). Seis melhorias de leitura/navegação no relatório (ao vivo e snapshots), **zero migration e zero dependência nova**: estornos sinalizados nas tabelas (T1 — linha esmaecida + "estornada", sem mudar contagem), **Δ dos KPIs com cor semântica** (T2 — verde/vermelho/cinza por indicador), busca livre + patrimônio→ficha (T3), KPI tiles clicáveis no ao vivo (T4), conteúdo escondido no mobile por chevron (T5), manutenção parada 30+ dias em vermelho + chip em Pendências (T6). Revisão de 5 lentes (1 achado de impressão corrigido). **993 testes.** Decisões em `docs/DECISOES.md` (2026-07-23 · F16); evidências em [`../RELATORIO-F16.md`](../RELATORIO-F16.md) |

| F17 | `F17-ultracode.md` | ✅ **CONCLUÍDA em 24/07/2026** (Frente A ∥ Frente B; descoberta do subsistema e revisão adversarial de 5 lentes por **workflow**, edições no loop principal com verificação `git diff` — hazard intermitente do OneDrive registrado). Duas frentes independentes, **zero migration e zero dependência nova**: **(A)** o **CI de banco de volta ao verde** — o roteiro `manutencao_fornecedor.sql` (cenário 4d) foi alinhado ao comportamento vigente da `0047` (o substituto do fornecedor nasce por `troca`, não `compra`), provado no ENSAIO, com regra nova no runbook ("rode TODOS os roteiros ao mexer em função/trigger/RPC"); **CI verde** (run `30089531148`, job `banco` incluso). **(B)** **legendas explicativas no relatório** para o operador **e** o visualizador por senha (Δ, estorno, quatro cores dos badges de manutenção, glossário "Como ler este relatório") + B5 (empty-state "encontrada", subtítulos nos tiles do grupo, nota "Troca", chip-âncora). Texto puro sem href; nenhuma contagem muda; corpo v1 e dashboard intactos. Revisão de 5 lentes (1 achado de gatilho corrigido). **1010 testes.** Decisões em `docs/DECISOES.md` (2026-07-24 · F17); evidências em [`../RELATORIO-F17.md`](../RELATORIO-F17.md) |
| F18 | `F18-ultracode.md` | ✅ **CONCLUÍDA em 24/07/2026** (Frente A banco → Frente B app ∥ Frente C docs em subagente → integração → revisão adversarial de 5 lentes por **workflow** → 2 achados corrigidos + re-revisão → rollout §R). A **pendência de item faltante** ganhou ciclo de vida próprio, **quatro migrations (`0050`–`0053`), zero dependência nova**: virou um **registro por ITEM** (`pendencias_item`) atado à devolução e ao **colaborador da época** — o ativo circula livre, a pendência não gruda nele nem passa ao novo dono; encerra por **desfecho manual** (recuperado/baixa), individual ou **em lote** (uma justificativa para vários — zerar a fila herdada); a resolvida fica na ficha (auditoria) e sai só da fila; **import não abre** pendência de item (precedente `0049`); o `triagem_ok` deixou de zerar o campo livre (corrige o apagão de trechos alheios). O trigger `aplicar_movimentacao` foi recriado com diff mínimo (devolucao→INSERT por item; estorno DELETA as linhas e não ressuscita o texto no restore, §A2); a fila/selo/chips/dashboard lêem a fonte única `v_fila_pendencias` (`v_pendencias` não-item ∪ `pendencias_item` aberto); FK `movimentacao_id` DEFERRABLE (o INSERT roda no BEFORE trigger). Revisão adversarial de 5 lentes: 2 achados reais (estorno ressuscitava o texto legado do snapshot; card do dashboard lia `v_pendencias`) **corrigidos** e re-revisados LIMPO. `lint`+`test`(**1018**)+`build` verdes; roteiro novo `pendencias_item.sql` (8 cenários) + `maquina_estados.sql` CENARIO 5 atualizado. Migrations aplicadas em **produção** por MCP (o `delete from pendencias_item` não bate no gate); backup dos afetados (`_f18_backup_pendencia`, RLS on) antes do backfill; smoke: 2 ativos com `%itens faltantes%` → 0, **3 abertas**, buckets não-item idênticos (`v_pendencias` 60→58, `v_fila` 61). **Limite: sem E2E visual autenticado** (login wall) — provado no nível de dados (as views/queries que a tela lê) + build/test. Decisões em `docs/DECISOES.md` (2026-07-24 · F18); evidências em [`../RELATORIO-F18.md`](../RELATORIO-F18.md) |
| F19 | `F19-ultracode.md` | ✅ **CONCLUÍDA em 24/07/2026** (orquestração multi-onda: gate → Onda 1 inventário de 7 áreas em **workflow** paralelo → Onda 2 provas ∥ Onda 3 produção read-only → Onda 4 correções → revisão adversarial de **5 lentes** por workflow → rollout §R). **Auditoria de conformidade** spec↔banco↔app↔produção, entregue como a matriz viva [`../MATRIZ-REGRAS.md`](../MATRIZ-REGRAS.md) — **209 regras**, cada uma com localização conferida e prova. Base **sólida**; **2 divergências confirmadas por SELECT em produção e corrigidas** por migration não-destrutiva (caminho A): **`0054`** (desempate não-determinístico do `rel_estoque_asof` no estoque as-of de período passado — **501→0** ativos em produção) e **`0055`** (`criar_compra_lote` authenticated-only, era executável por anon/service_role). Emendas de doc (spec §5 fórmula de `falta`; ARQUITETURA §3/§9); decisão registrada (regra 3 Zod-only). **5 roteiros SQL novos** (inclusive o 1º da RPC de import) **+ 3 Vitest de função pura (1018→1059)**; produção conferida **100% read-only**; smoke 50 OK · 0 falha. Decisões em `docs/DECISOES.md` (2026-07-24 · F19); evidências em [`../RELATORIO-F19.md`](../RELATORIO-F19.md) |
| F20 | `F20-ultracode.md` | ✅ **CONCLUÍDA em 24/07/2026** (Onda 0: 3 inventários read-only ∥ por **workflow** → gabarito `docs/PLANO-AJUDA.md` → Onda 1 motor, sequencial no loop principal → Onda 2: 4 frentes de conteúdo ∥ com arquivos disjuntos → revisão adversarial de **5 lentes** ∥ → emendas em 4 frentes ∥ → **re-revisão de 3 lentes** → rollout §R). A `/ajuda` — **um scroll de 56 KB** desde a F6B — virou uma **seção de documentação com 33 páginas** organizada por intenção (*Comece aqui · Como fazer · Consultar · Resolver*), **zero migration, zero script de banco e zero dependência nova**. O inventário mediu o problema: das **103 capacidades** entregues da F6B à F19-UX, **25 estavam ausentes e 11 desatualizadas** — 1 em cada 3 —, todas fechadas com os **rótulos reais das telas lidos no código** (modo escuro, painel inicial e KPIs clicáveis, chamado do fornecedor, devolução ao fornecedor com vínculo de sucessão, ações do menu ⋯, import com correção em massa, convite por link na tela com nome e sobrenome, **58 mensagens de erro** traduzidas para "o que fazer" e **16 sintomas** com saída). Motor: registry tipado como fonte única de rotas/índice/busca/testes, `/ajuda/[slug]` + `/ajuda/manual` imprimível, 5 tipos de bloco novos, busca no índice, grupo "Ajuda" no `Ctrl+K` servido por índice serializado no servidor (o cliente nunca importa o conteúdo), o "?" contextual em **16** telas (eram 8) e **compatibilidade total das âncoras antigas**. O que impede a documentação de envelhecer virou build: **rota nova sem página, `LinkAjuda` órfão, referência cruzada quebrada, jargão de dev, promessa de futuro, patrimônio não-fictício ou página fora do smoke QUEBRAM o teste** — e o `conteudo.test.ts` da F9→F18 atravessou a fase **com uma linha alterada** (e para mais forte), rodando sobre uma visão de compatibilidade que prova, no build, que **nada do manual antigo se perdeu**. Revisão adversarial: **37 achados** (3 graves) + **14** na re-revisão = **51 resolvidos**, um refutado com prova; entre eles **dois defeitos de código da própria fase** — o "?" de `/relatorios/gerados` expulsava o visualizador por senha para `/login`, e a lista branca das âncoras herdava o `Object.prototype` (`/ajuda#toString` derrubava o índice). A revisão também converteu a matriz de cobertura em **executável** (revelando 3 abas de admin apontando errado) e o TRAP do só-servidor em **varredura automatizada**, provada nos dois sentidos. `lint`+`test`(**1446**, eram 1125)+`build`(**26 rotas**) verdes; smoke autenticado estendido para conferir as 33 páginas por 200 **+ marcador de conteúdo** — **86 OK · 0 falha**. **Limites: sem screenshot** (o painel do navegador não estava exibido — as provas visuais são medições no DOM) e **sem `gh` nesta máquina** (CI por verificar). Decisões em `docs/DECISOES.md` (2026-07-24 · F20, duas atas); mapa vivo em [`../PLANO-AJUDA.md`](../PLANO-AJUDA.md); evidências e "o que este relatório NÃO prova" em [`../RELATORIO-F20.md`](../RELATORIO-F20.md) |
| F20B | `F20B-ultracode.md` | ✅ **CONCLUÍDA em 28/07/2026** (gate → exploração de 3 frentes ∥ por **workflow** → `PLAN.md` → 2 incrementos sequenciais → revisão adversarial de **4 lentes** com refutador por achado ∥ → verificação em navegador → rollout §R). Duas queixas do Johnny, ambas cirúrgicas. **(1) Nome do arquivo do termo:** o `.docx` baixava como `Responsabilidade Notebook - Fulano-de-Tal.docx` — sem patrimônio e com o nome hifenizado — e passou ao padrão oficial `<tipo> - <patrimônio(s)> - <colaborador>.docx`, com acentos e espaços preservados e a **ordem do documento** mantida na devolução em lote; a lógica virou função pura `nomeArquivoTermo` em `src/lib/termos/nome-arquivo.ts` com **mapa de prefixos próprio**, desacoplado de `TERMO_ROTULO` (rótulo de UI que também alimenta a ajuda — antes, mexer numa tela renomeava downloads em silêncio). **Termo gerado antes da ordem baixa com o nome novo** (calculado na hora; zero toque em banco/Storage). **(2) "Tentar novamente" que não tentava:** os 5 `error.tsx` chamavam `reset()` puro, que no App Router não refaz as leituras de Server Component — passaram a usar **`unstable_retry`** (Next ≥ 16.2), a forma que a doc oficial manda hoje (a ordem previa o hand-roll `refresh()+reset()`; conferido no pacote instalado, `unstable_retry` **é** exatamente isso), via o componente compartilhado `TentarNovamente` com estado de progresso e **fallback** caso a API `unstable_` seja renomeada. **O limite declarado pela ordem ("clique real não é automatizável, login wall") não se sustentou:** com o proxy de sessão da F20, o clique foi provado em **navegador de verdade na tela logada**, em **A/B controlado** — boundary novo + causa resolvida ⇒ recupera **sem F5** (variável de documento preservada = sem reload); boundary antigo, mesmas condições ⇒ 15 s de cliques sem efeito, só o F5 salva. O nome foi conferido ponta a ponta em **termos reais de 27–28/07**, interceptando o `a.download` sem baixar e sem expor dado. Revisão adversarial: **7 achados, todos refutados** como pendências de encerramento previstas (0 defeito de corretude); o único defeito real da fase saiu de uma sonda própria — o corte do teto de 150 partia **par surrogate** ao meio. Sonda read-only nos **dois** projetos: produção tem as 6 linhas de `termos_gerados` com a chave certa e **zero divergência** jsonb×coluna. `lint`+`test`(**1496**, eram 1457)+`build` verdes. **Zero migration, zero escrita em banco, zero dependência nova.** **Limites: sem screenshot** (provas são medições no DOM) e **sem `gh` nesta máquina** (CI por verificar). Decisões em `docs/DECISOES.md` (2026-07-28 · F20B); evidências e "o que este relatório NÃO prova" em [`../RELATORIO-F20B.md`](../RELATORIO-F20B.md) |

| F21 | `F21-papeis-ultracode.md` | ✅ **CONCLUÍDA em 29/07/2026** (descoberta de 6 frentes ∥ por **workflow** → camada de acesso no loop principal → banco `0061`–`0068` aplicado em ensaio e produção → 4 frentes de implementação ∥ em arquivos disjuntos → integração → revisão adversarial de **5 lentes com refutação por achado** → correções `0067` + 3 de app → **re-revisão de 3 lentes** → `0068` + 2 correções → re-verificação → rollout §R). O fim do **nível único**: três cargos com hierarquia (**admin ⊃ operador ⊃ consulta**) e **vínculo de filiais de escrita** por operador, com a regra valendo no **Postgres** — 8 migrations, enum `papel_usuario`, `operador_filiais`, as funções `papel_atual`/`e_admin`/`pode_escrever_filial`, a troca de **todas** as policies de escrita, a guarda `e_admin()` **dentro** da RPC do import (`security definer`, passa por fora de policy), `eventos_admin` (auditoria insert-only) e as policies dos **buckets de Storage** — mais **desativar usuário** com efeito no request seguinte, `/admin/usuarios` completa (convidar com cargo+filiais, editar, desativar/reativar, autoproteções, aba Auditoria) e o gating da UI. Leitura segue **ampla para todos** (ADR-001 mantida nesse ponto). **Três premissas da ordem caíram na medição** e estão registradas: `senhas_acesso` já estava fechada pela `0012` e fechar com `e_admin()` seria **afrouxar**; `criar_compra_lote`/`devolver_ao_fornecedor` são `security INVOKER` (a única secdef de escrita alcançável era a do import); e `(select ...)` não gera InitPlan para função com argumento de LINHA. **Dois achados fora do mapa da ordem:** as 8 policies de `storage.objects` não olhavam cargo (`0066`), e — o mais grave — a policy de `movimentacoes` gateava a filial que o **cliente declara**: exploit **reproduzido no ensaio** (operador vinculado só à filial 1 moveu para si um ativo da filial 2 mentindo um campo, porque o trigger que aplica a movimentação é `security definer`), corrigido na `0067` gateando a filial de **origem lida do banco** (`snapshot_anterior`). Revisão adversarial: **16 achados brutos → 4 confirmados** (1 alta por 2 lentes, 1 média por 3) + **8 refutados com prova**; os outros três confirmados eram o INSERT de `import_logs` aberto a qualquer logado, o desligado caindo na porta **pública** do visualizador em vez do login, e o diálogo "Editar" abrindo **quebrado para todos os 9 usuários** (o backfill dá vínculo em todas as filiais e o validador recusa lista não vazia para admin). `papeis_rls.sql` **41 → 47 asserções** (as novas são a lacuna que deixou o furo passar; as de leitura passaram a plantar linha nas tabelas fechadas, porque "ver 0 numa tabela vazia" não prova nada), **47/0 nos dois bancos**. Advisor `rls_policy_always_true` **12 → 0**. Deploy como **não-evento**: backfill → todos admin com todas as filiais; smoke do app **antigo** contra o banco **novo** = **86 OK · 0 falha**; acervo intocado (1.230/2.361). `lint`+`tsc`+`test`(**1.588**, eram 1.496)+`build`(26 rotas) verdes. **Limites: a UI dos cargos não foi vista em navegador** (não existe usuário não-admin em nenhum dos dois bancos — é o roteiro manual de 5 min do relatório), `db:seed` **não rodou** (o `.env.local` desta máquina aponta para produção e a guarda recusa) e **sem `gh`** (CI por verificar). Decisões em `docs/DECISOES.md` (2026-07-29 · F21, seis atas); ADR em [`../ADR-002-papeis-e-permissoes.md`](../ADR-002-papeis-e-permissoes.md); evidências em [`../RELATORIO-F21.md`](../RELATORIO-F21.md) |

| F22 | `F22-cargo-dev-ultracode.md` | ✅ **CONCLUÍDA em 30/07/2026** (orquestração multiagente: banco `0071`–`0077` → camada de acesso e gestão de usuários ∥ área `/dev` ∥ documentação → integração e encerramento). O **quarto cargo**, `dev` (rótulo "Desenvolvedor"), no TOPO da hierarquia — **dev ⊃ admin ⊃ operador ⊃ consulta** —, com as **sete migrations do desenho (`0071`–`0077`) aplicadas em ensaio e produção**, mais a `0078`, **também aplicada nos dois bancos** (endurecimento pedido pela leitura de advisors **depois** do apply: `exigir_gestao_de` e `existe_outro_admin_ativo` saíram da API pública, porque só são chamadas por dentro das RPCs), e **zero dependência nova**. Três propriedades: (1) o dev faz tudo que o admin faz, e herda a maior parte **sem policy nova**, porque `e_admin()` passou a significar "nível **administrador**" (`admin` ou `dev`) — a herança tinha dois buracos que a medição achou e a `0072` tapou: `pode_escrever_filial()` tinha o seu próprio `= 'admin'` (um dev perderia TODA a escrita de acervo, e **em silêncio**, porque o `USING` de uma policy de UPDATE é filtro de linha, não erro) e **cinco** policies gateavam por lista literal `in ('admin','operador')` (anotações, relatórios gerados e as três de escrita do bucket dos termos), que redefinição de função nenhuma alcança — agora chamam a função nova `pode_escrever()`; (2) **ninguém abaixo de dev tem poder sobre um dev**, e a recusa vale no **Postgres**: o trigger `profiles_guarda_dev` (`0073`) barra por padrão toda mexida em linha dev e toda concessão do cargo, **inclusive vinda do service role**, e as **RPCs de gestão** (`0074`: cargo, status, vínculos, apagar, encerrar sessões — chamadas com a sessão de quem clicou, tirando o service role do caminho de gravação do acesso) abrem a única janela que passa; (3) a área **`/dev`** (diagnóstico com migration aplicada × repositório, **sete checagens de integridade só-leitura**, auditoria completa com export, manutenção) — **sem console de SQL**, e a RPC das checagens com o SQL **fixo por dentro**, porque receber a consulta como parâmetro é execução arbitrária com os privilégios do dono da função. **Apagar usuário = perfil ARQUIVADO** (`excluido_em`, FK para `auth.users` derrubada): a conta some do login para sempre, o e-mail volta a ficar livre e **a autoria histórica fica intacta** — o caminho ingênuo era impossível (dez FKs de histórico com `NO ACTION`, e o cascade fazia `deleteUser()` falhar); consequência aceita: apagar conta pelo painel do Supabase passa a deixar perfil órfão, o que virou checagem de integridade. **Três medições que mudaram o desenho:** `set_config(k,v,true)` é local à **transação**, não à chamada (por isso toda RPC fecha a janela ao sair); o `admin.signOut()` do supabase-js instalado (**2.110.2**) recebe um **JWT**, não um id de usuário — não existe caminho pela API para revogar sessão de terceiro, daí a RPC e o limite de ~1h declarado na tela; e a oitava checagem candidata ("status do ativo diverge da última movimentação") foi **descartada** por acusar **1009 de 1231** ativos em produção — artefato de ordenação da compra de abertura do import, não inconsistência. Documentação: spec §3 (quatro cargos), **emenda datada** na [`../ADR-002-papeis-e-permissoes.md`](../ADR-002-papeis-e-permissoes.md) (§13 "Cargo dev — 30/07/2026") e `CLAUDE.md`; onze atas em `docs/DECISOES.md` (2026-07-30 · F22); evidências, limites e "o que este relatório NÃO prova" em [`../RELATORIO-F22.md`](../RELATORIO-F22.md) |

| F23 | `F23-dev-destrutivo-ultracode.md` | ✅ **CONCLUÍDA em 30/07/2026** (orquestração multiagente: mapeamento dos 7 subsistemas ∥ roteiro SQL → banco `0079`–`0090` → camada de app e UI → correções da prova → encerramento). As **ferramentas DESTRUTIVAS do cargo dev**, numa **Zona destrutiva** própria (`/dev/destrutivo`, e não um card na `/dev`: tudo lá é seguro de clicar e nada aqui é), com as **DOZE migrations aplicadas em ensaio e produção** e **zero dependência nova**. Três famílias: **apagar** (ativo com todo o rastro · a **última** movimentação · item com lançamentos), **resetar** (acervo e/ou lançamentos; uma filial ou global) e **forçar** (estado de ativo · saldo de item). Toda operação: confirmação digitada validada **na action E na RPC**, justificativa obrigatória, **backup** e trilha gravada **dentro da mesma transação** — se a trilha falhar, nada é apagado. **O achado que reorientou a fase:** o critério 7 mandava *preservar* a recusa de UPDATE/DELETE direto "inclusive para o service role", e a medição mostrou que **essa garantia não existia** — a imutabilidade era a **ausência** de policy de UPDATE/DELETE, o que segura `authenticated` e não segura quem tem `rolbypassrls` (com os grants amplos de tabela que o Supabase concede por default, e o app tem um client de service role). Foi preciso **construí-la**: a `0081` instala um **trigger** que recusa por padrão e só passa pela janela `estoque.dev_destrutivo` (GUC local à transação, fechada ao sair — inclusive em erro), no molde da `0073`. Provado **em produção**, dentro de `begin/rollback` para que nem uma falha da guarda tocasse dado real: como service role, `delete`/`update` em movimentações, `delete` em ativos e lançamentos e `insert` com a marca → **todos 42501**, e o INSERT legítimo **continua passando**. **O defeito que a prova encontrou:** `created_at` tem default `now()`, que é o mesmo instante para linhas da MESMA transação — e o import grava a compra de abertura e o ajuste juntos; "a última movimentação" desempatava por `(created_at, id)`, isto é, por **uuid aleatório**, e apagar a errada restaurava o ativo do snapshot **anterior ao próprio nascimento**. Medido em produção: **1111 dos 1232 ativos** (mesma causa que a `0054` já diagnosticara para o as-of). A `0087` **RECUSA o empate** em vez de eleger um desempate — operação irreversível não se decide por palpite. **Duas leituras que a ordem supunha e o código desmentiu:** kits **não** citam itens (o payload traz `categoria_ativo[]`), então o §2.3 era vazio; e a correção-dev não precisa de exceção nova em relatório — dos **20** consumidores de movimentação, **19** já a excluem por allow-list de tipo, e o único vazamento era o card "Últimas movimentações" do dashboard. **`scripts/reset.ts`** passou a chamar uma RPC nomeada (a guarda recusaria os `.delete()` por service role); a proteção anti-produção continua sendo o `env-guard`. **1.667 testes** (eram 1.647), roteiro `dev_destrutivo.sql` com **108 asserções** e os **7 roteiros vizinhos verdes** (CI `banco` verde num Postgres novo); smoke pós-deploy **88 OK · 0 falha**. **Nada do acervo de produção foi tocado** — a fase instala as ferramentas; usá-las é decisão do dev. Backlog aberto: o import **não apaga `pendencias_item`** (bug preexistente da F18, latente porque produção tem zero linhas ali). Emenda de arquitetura no §14 da [`../ADR-002-papeis-e-permissoes.md`](../ADR-002-papeis-e-permissoes.md); **21 atas** em `docs/DECISOES.md` (2026-07-30 · F23); evidências, roteiro manual de 5 minutos e "o que este relatório NÃO prova" em [`../RELATORIO-F23.md`](../RELATORIO-F23.md) |
| F24 | `F24-import-conflito-filiais-ultracode.md` | ✅ **CONCLUÍDA em 30/07/2026** (orquestração multiagente: mapeamento paralelo dos 6 subsistemas → banco `0091`–`0098` → motor do import → mesa e exclusão → efeitos colaterais → encerramento). **O conflito entre filiais deixa de bloquear o import e ganha mesa de resolução**, com as **OITO migrations aplicadas em ensaio e produção** (a `0098` nasceu da revisão adversarial) e **zero dependência nova**. A identidade do ativo virou **por filial** (`0091`: os dois índices únicos ganharam `filial_id`; os NOMES ficaram, porque `erros.ts` casa o 23505 pelo nome e renomear mataria a tradução em silêncio). O conflito é **DERIVADO** (`0092`/`0096`), nunca gravado — some sozinho quando o grupo se desfaz; a derivação venceu a materialização **por medição**: `EXPLAIN` sobre os 1.232 ativos reais deu **5,2 ms a quente**. A mesa em `/pendencias` mostra os cadastros **lado a lado**, com **diff realçado** e o resumo de histórico de cada lado (separando movimentação **real** da carga do import, pelo marcador que a RPC grava). **A exclusão é presa ao conflito** (`0093`): alcança exclusivamente ativo em conflito **naquele instante**, é **all-or-nothing** (um id fora recusa tudo), **trava o GRUPO INTEIRO** antes de revalidar — travar só a seleção deixaria o **gêmeo** sumir no meio —, exige `APAGAR <N>` (com deduplicação: id repetido não infla a contagem) e justificativa nas duas camadas, e grava **backup jsonb + trilha na MESMA transação** (cap de 25 ativos, medido: média de 2.294 B, máx. 5.864). **A única exclusão de ativo fora da Zona destrutiva do dev** — e o roteiro prova que o admin **não** alcança `apagar_ativo`. **O furo que a auditoria da §1.4 encontrou:** `corrigirPatrimonio` e `definirServiceTag` não consultavam nada — dependiam do 23505 do índice global — e passariam a **abrir conflito em silêncio a partir da ficha**; as duas ganharam a checagem global explícita. **O efeito colateral que a ordem não previa:** além da transferência para a filial do gêmeo (prevista, `0097`), o **estorno** de uma transferência antiga tem o mesmo buraco — devolve o ativo à filial de origem, que pode ter ganhado outro cadastro; a guarda cobre os dois, e a asserção 7b prova que a recusa é **estreita**. **A armadilha de UI que teria apagado o que a fase quer importar:** `existe_em_outra_filial` entrava no lote de "Aplicar todas as correções" como "só-remover"; com a remoção virando opcional, um clique ali apagaria justamente as linhas que agora devem entrar. **Duas falhas silenciosas fechadas por TIPO:** `FiltrosPendencias.tipo` passou a excluir `'conflito'` (senão a query sairia **sem filtro**, devolvendo a fila inteira — a mesma classe de defeito de 25/07) e o export CSV da aba ganhou fonte própria. **1.697 testes** (eram 1.667), roteiro `conflito_filiais.sql` com **38 asserções, 0 falhas**, e os roteiros vizinhos verdes. **Nada do acervo de produção foi tocado** (1.232 ativos e 2.377 movimentações antes e depois) — a fase instala a mesa; usá-la é decisão de quem opera. Três afirmações da **ajuda** que a fase tornou falsas foram corrigidas (nenhum teste as pegava). **8 atas** em `docs/DECISOES.md` (2026-07-30 · F24); evidências, roteiro manual de 5 minutos e "o que este relatório NÃO prova" em [`../RELATORIO-F24.md`](../RELATORIO-F24.md) |
| F25 | `F25-celular-cidade-filtros-ultracode.md` | ✅ **CONCLUÍDA em 04/08/2026** (orquestração multiagente: mapeamento paralelo dos 6 subsistemas → banco `0101`/`0102` + retag dos modelos → celular ∥ cidade ∥ filtros → testes e documentação → revisão adversarial em contexto fresco). **Três melhorias de uso, independentes entre si**, com as DUAS migrations **aditivas** aplicadas em ensaio e produção e **zero dependência nova**. **(A) O celular ganhou campos próprios:** nº do telefone, IMEI e Pulsus saíram de `ativos.observacoes` (texto livre, redigitados a cada termo) e viraram colunas (`0101`), exibidas **só** quando a categoria é celular, no CSV de ativos e **pré-preenchendo** o termo — fecha um dos itens que o `PLANO-TERMOS` §9 listava fora de escopo desde a F5A. No **lote** eles somem de propósito: não existe formulário "single" neste app, os campos de "Dados do modelo" são compartilhados, e um IMEI compartilhado gravaria o mesmo número em vinte aparelhos. São gravados por **UPDATE pós-RPC**, porque `criar_compra_lote` lê chaves nominais do jsonb e acrescentar as novas sem recriá-la seria **no-op silencioso** (recriar a RPC contraria o §1.3 da ordem e dispara a regra F17 do runbook); o preço aceito é o UPDATE ficar fora da transação da compra, e por isso o erro é logado e **não derruba o cadastro** — devolver falha faria o operador repetir uma compra que já aconteceu. **(B) A cidade do termo saiu do template:** `filiais.cidade` (`0102`, semeada por **slug**, não por id — id de filial não é garantido igual entre os bancos) e a linha da assinatura dos 7 modelos virou `{cidade}, {data_extenso}` — responde a **pergunta aberta nº 4** do `PLANO-TERMOS` §10, de 14/07. A **cláusula de foro ficou intocada** (decisão do Johnny: a assinatura diz onde se assinou; o foro é escolha jurídica da sede). O retag foi feito por `scripts/termos/retaguear-cidade.mjs`, que **prova** a fidelidade em vez de prometê-la — a linha é um **run único** nos 7 (medido: não houve mesclagem de runs a fazer, que é a armadilha clássica do retag do Word), e o script exige que só `word/document.xml` divirja no pacote inteiro e conta a ocorrência do foro antes/depois; 7 modelos × 4 cidades renderizados com payload fictício, 28 assinaturas conferidas. O buraco fechado no caminho: `nullGetter` faz chave ausente render **vazio**, e nenhum termo salvo antes da fase tem `cidade` — reabrir um deles sairia **começando por vírgula**; o diálogo passou a mesclar o preparado **sob** o snapshot. **(C) Filtro de filial multi-seleção com padrão por cargo:** painel de caixas nas 5 listas, operador entrando com as filiais vinculadas dele já marcadas, aba de `/relatorios` por cargo e `/itens` abrindo lado a lado. O ponto sutil foi a URL: o param ganhou **três** estados (ausente = padrão do cargo · `todas` = sentinela · lista = igual para todos), porque sem a sentinela o operador não teria como pedir "todas"; consequência aceita e registrada — **link sem o param muda de sentido conforme quem abre**. A regra mora num lugar só, decide pelo **cargo** (nunca por "lista de vínculos vazia", ambígua entre consulta e operador quebrado), e a tela e o **CSV** resolvem pela MESMA função, porque o export reparseia a querystring e divergiria em silêncio. **Duas contas que a multi-seleção quebrava:** a contagem de conflitos contaria o mesmo grupo **duas vezes** (o invariante da F24 vale POR filial) e o rótulo do CSV de saldos diria "Consolidado" num arquivo somando duas filiais. **Nenhuma permissão mudou** — filtro é leitura; RLS, policies e os selects de escrita intocados. O **import** ficou intocado, e a prova é o tipo: o diff do motor é **vazio** e só a anotação `Filial` → `Pick<Filial,…>` mudou, que é apagada no build. **1.772 testes** (eram 1.697); **nada do acervo tocado** (1.708 ativos, **0** com os campos novos — a fase **não** faz backfill de `observacoes`, por decisão registrada: mover texto livre é escolha humana, na ficha). Um guarda da ajuda F20 **pegou de verdade** (uma frase literal exigida por teste, partida por um aparte na reescrita); as demais correções de documentação não quebravam teste nenhum — o tipo de lacuna mais perigoso. **14 atas** em `docs/DECISOES.md` (2026-08-04 · F25); evidências, roteiro manual de 5 minutos e "o que este relatório NÃO prova" em [`../RELATORIO-F25.md`](../RELATORIO-F25.md) |
| F26 | `F26-troca-upgrade-ultracode.md` | ✅ **CONCLUÍDA em 04/08/2026** (orquestração multiagente: mapeamento paralelo dos 5 subsistemas periféricos → regra pura do par + Vitest → UI dos passos 2 e 3 ∥ painel de sucesso ∥ rascunho → ajuda e documentação → duas voltas de revisão adversarial em contexto fresco, a segunda contra o próprio conserto). **O facilitador do par troca/upgrade**, com **ZERO migration** — o diff de `supabase/` é vazio — e **zero dependência nova**. Trocar o equipamento de alguém sempre foram DUAS movimentações (a `devolucao` do antigo e a `saida` do novo, as duas com o **motivo** `troca_upgrade`) e custava duas passadas completas pelo fluxo, redigitando colaborador e contexto; agora a mesma tela abre a metade oposta e as duas entram num **único registrar**. **(A) A seção é DERIVADA, não um modo:** aparece e some a partir do tipo + do **CÓDIGO** do motivo (`troca-upgrade.ts`, constante única `MOTIVO_TROCA_UPGRADE`), então vale igual vindo do select, de um **kit**, do **"repetir última"** ou do **"duplicar"** — e trocar tipo/motivo limpa a seção **E o estado dela** (`sincronizarContrapartida` é chamada por todos os caminhos que trocam tipo/motivo; `ajustarTipoPara`, `restaurarRascunho` e `reiniciar` zeram a contrapartida explicitamente), para não sobrar lote órfão invisível. Detectar por **rótulo** era o defeito à espreita: o admin renomeia "Troca / upgrade" em `admin/motivos` quando quiser. **(B) O prefill é honesto:** o colaborador da metade que entrega só é pré-preenchido quando **TODOS** os devolvidos estão com a **mesma** pessoa — misto ou vazio deixa o campo em branco —, e o nome sai do lote **em memória, capturado antes do envio**, porque depois do insert o trigger `aplicar_movimentacao` já zerou o detentor (ler dali daria vazio **sempre**). **(C) As guardas do par valem nos dois lados:** metades disjuntas, teto **SOMADO** com a mensagem derivada da constante, contrapartida aberta-e-vazia bloqueando o registrar, e cada ativo checado contra o tipo da **sua** metade — recusado na entrada com toast que nomeia o culpado **e** re-checado no envio (o caso real é o **rascunho que dormiu** enquanto outro operador movimentava aquele ativo). No servidor nada mudou: a barreira de ativo repetido, o teto do schema e o `exigirEscritaEm` das filiais tocadas já cobriam o lote heterogêneo. **(D) A ordem das metades é decisão:** a principal vai **primeiro** no array, porque a action interrompe no primeiro erro — assim uma falha na metade montada impede a contrapartida de entrar sozinha, que é a meia-troca invertida que o facilitador existe para evitar. **A armadilha que quase comeu o atalho do "deixar para depois":** `/movimentacoes/nova` → `/movimentacoes/nova?tipo=…` é a **mesma rota**, e soft navigation do Next **não remonta** o formulário — todo o estado inicial vem de `useState(() => …)`, que só roda na montagem, então o atalho abriria a tela sem pré-preencher **nada**, em silêncio; a correção é uma `key` derivada dos params na página. E o link carrega `contrapartida=nao`, sem o qual a tela pediria a contrapartida **da contrapartida** — um laço que ainda por cima bloqueia o registrar. **Duas voltas adversariais, e a segunda achou defeito no conserto da primeira:** o marcador que impede o laço nasceu como estado de MONTAGEM, e por isso não sobrevivia ao rascunho (restaurar numa URL limpa ressuscitava o laço) e era herdado por um par NOVO montado na mesma tela; virou campo do **par** (`contrapartida.jaRegistrada`), persistido junto com o `deixarParaDepois`. Na mesma volta: o **prefill congelava** no instante em que a seção nascia — juntar ao lote um equipamento de outro detentor deixava o nome errado escrito num lote MISTO —, e passou a acompanhar o lote enquanto o campo for do sistema (`sincronizarPrefill`). **1.860 testes** (eram 1.778), **+1 arquivo** de teste; quatro módulos novos no wizard, dois deles **extraídos** de código que já existia (chips Hoje/Ontem e checklist de acessórios), para as duas metades usarem a mesma peça. O smoke ganhou **marcador de conteúdo** em `/movimentacoes/nova` — a rota só exigia 200, e um 200 com o formulário quebrado continuaria verde. **O par não é gravado como par** (decisão do Johnny, "só a tela"): são uma `saida` e uma `devolucao` normais, os relatórios contam como sempre contaram, e **estornar uma metade não desfaz a outra**; vínculo no banco e pendência de troca incompleta ficaram no **backlog**. O tipo `troca` (F15), a RPC `devolver_ao_fornecedor` e o fluxo de devolução ao fornecedor ficaram **byte a byte**. **9 atas** em `docs/DECISOES.md` (2026-08-04 · F26); evidências, roteiro manual de 5 minutos e "o que este relatório NÃO prova" em [`../RELATORIO-F26.md`](../RELATORIO-F26.md) |
| F27 | `F27-onda-a-ux-ultracode.md` | ✅ **CONCLUÍDA em 07/08/2026** (Onda A da análise de UX de 07/08 — `docs/ANALISE-UX-2026-08-07.md` §10 —, trabalho direto na `main`, modo autônomo, seis blocos sequenciais + revisão adversarial de 6 lentes em contexto fresco). **26 itens**, **zero migration, zero dependência nova**: `next` sobrevive à sessão expirada nas duas portas (operador e visualizador por senha, sanitizado como no convite), título por página (WCAG 2.4.2, patrimônio na aba da ficha), erro do passo 2 do wizard rola a tela e recebe foco, teto de 30 também no combobox um-a-um, busca de `/ativos` ganha service tag/hostname/telefone/IMEI, termo herda a data da movimentação retroativa, observação sai inteira no papel, confirmação digitada ganha a mesma dica nas três telas que já a exigiam, e a `/dev` para de descartar em silêncio 2 das 9 checagens que a RPC já devolvia. Revisão adversarial: **3 achados, todos corrigidos** (a régua de confirmação digitada só alcançava 2 das 3 telas; o "repetir última" vazava para outros 3 caminhos que zeravam tipo sem limpar motivo/termo/data). **O smoke pós-deploy pegou o que build, testes e as 6 lentes não pegaram:** `/dev/destrutivo` respondia 200 com `<title>` "Zona destrutiva" para quem era barrado — vazamento só de título (o corpo era idêntico ao de `/dev`, sem ferramenta destrutiva nenhuma renderizada) —, corrigido revogando, na própria fase, a decisão de títulos distintos que ela tinha acabado de tomar. **1.899 testes** (eram 1.865). Decisões em `docs/DECISOES.md` (2026-08-07 · F27); evidências, checklist dos 26 itens e "o que este relatório NÃO prova" em [`../RELATORIO-F27.md`](../RELATORIO-F27.md) |
| F28 | `F28-onda-b1-operacao-ultracode.md` | ✅ **CONCLUÍDA em 07/08/2026** (orquestração multiagente: 9 frentes simultâneas com dono exclusivo por arquivo, 16 agentes, 0 colisão de escrita). **22 itens** da rotina diária do operador — wizard e lista de movimentações, ativos, pendências, itens —, **zero dependência nova**: o passo 3 do wizard ganha o card "o que será gravado" com a data em destaque, aviso antecipado de vínculo de filial já no passo 1, chips Hoje/Ontem/7 dias + filtro "Minhas" em `/movimentacoes` (uid resolvido no servidor, nunca na URL), "Duplicar" na própria linha, rascunho de compra em `sessionStorage`, pendência visível na lista de ativos com coluna no CSV, mesa de conflitos avisando que marcar é **condenar** o cadastro, confirmar assinatura de termo em lote, e reabrir pendência de item pelo nível administrador. Revisão adversarial em duas voltas, **12 achados corrigidos** — CSV divergindo da tela em dois filtros novos (campo opcional é ponto cego de tipo; TS/lint/testes ficaram verdes) e o aviso mais importante da mesa de conflitos reprovando contraste AA. **O CI achou um 13º achado que a revisão não viu:** na primeira execução real do roteiro SQL novo, `authenticated` não tinha `GRANT` nenhum sobre `pendencias_item` num banco construído só pelas migrations — a migration **`0103`** fecha o grant e separa a policy de UPDATE por sentido (operador só abre→resolve; só nível administrador resolve→abre), **aplicada em ensaio e produção** depois que o MCP de banco foi conectado (a medição prévia desmentiu duas suposições do próprio relatório: produção já tinha o grant pelo bootstrap do Supabase, e tinha 5 linhas em `pendencias_item`, não 0 como um comentário de 30/07 dizia). **2.041 testes** (eram 1.899). Decisões em `docs/DECISOES.md` (2026-08-07 · F28); evidências, checklist dos 22 itens e "o que este relatório NÃO prova" em [`../RELATORIO-F28.md`](../RELATORIO-F28.md) |
| F29 | `F29-onda-b2-relatorios-admin-ultracode.md` | ✅ **CONCLUÍDA em 07/08/2026** (mapeamento paralelo de 7 leitores só-leitura → `PLAN.md` → 4 frentes disjuntas em sequência REL/ADM/UXG/alvos-de-toque → revisão adversarial de 5 lentes com cético por achado). **18 itens** fechando a Onda B — relatórios, administração, base global —, **zero dependência nova, `supabase/` intocado**: preset "Semana passada" (a dualidade das duas semanas — na tela × no diálogo de congelar — fica de pé, deliberada e travada por teste), diálogo de snapshot abrindo com o período ativo da tela + aviso de versão já existente + retry em corrida de versão, paginação de 30 em `/relatorios/gerados` com badge "superada" exata, tooltip nos gráficos empilhados/divergentes, Δ dos KPIs citando o valor anterior, "Copiar texto" ganhando o bloco "Em estoque (N)" que faltava, âncoras que abrem grupo recolhido no celular, "aguardando primeiro acesso" + "Gerar novo link" no convite, filtro e ordenação nas tabelas administrativas, preview + duplicar de kit, "Testar senha…" (timing-safe, sem log), alvos de toque de 40px, navs roláveis com degradê, `role="alert"` nas caixas mudas e **contraste no CI** (17 pares novos, todos medidos antes de virarem exigidos, zero known-fail novo). A ordem supunha corrida de versão sem lock; a medição achou a constraint **já existente desde a F3** (`0010`/`0013`) — o defeito real era perda de trabalho num erro genérico, não duplicata, sem migration nenhuma. Revisão adversarial: **6 achados, 4 confirmados** (o "Copiar texto" ficava mudo nos snapshots v1; uma leitura de banco que falha virava a mensagem específica "outra pessoa gerou este período", mentindo sobre uma queda de infraestrutura) e 2 refutados com prova. **2.121 testes** (eram 2.041). Decisões em `docs/DECISOES.md` (2026-08-07 · F29); evidências, checklist dos 18 itens e "o que este relatório NÃO prova" em [`../RELATORIO-F29.md`](../RELATORIO-F29.md) |
| F30 | `F30-onda-c1-selecao-impressao-sidebar-ultracode.md` | ✅ **CONCLUÍDA em 09/08/2026** (três recursos independentes, sem nenhum toque em banco; Onda C1 do `BACKLOG-UX.md`). **Zero migration, zero dependência nova**: **ATV-03** dá à lista de ativos uma coluna de seleção (TanStack row selection, `aria-label` por patrimônio, cabeçalho `indeterminate`) com barra sticky "N selecionados · Movimentar · Copiar patrimônios · Limpar seleção" e `/movimentacoes/nova?ativos=…` resolvendo o lote no servidor, deduplicado e cortado no teto de 30; **REL-01** cobre as **57 ocorrências** de coluna escondida por breakpoint nas seis tabelas do relatório com uma variante `print:`, medindo 685px de tabela numa página A4 de 718px sem regredir a observação completa da F27; **UXG-13** dá botão + tecla `[` para recolher a sidebar (240px→64px, +176px de conteúdo), tooltip por hover e por foco no modo só-ícones, separadores de grupo (nunca no primeiro item da lista já filtrada por cargo) e preferência por `localStorage` sem flash (atributo no `<html>` escrito por script inline antes da pintura). Revisão adversarial de 4 frentes achou **2 defeitos reais**: o modo só-ícones vazava regra de CSS para o Sheet do menu mobile (as três regras não estavam ancoradas no `<aside>`) e a sincronia entre abas via evento `storage` não existia de fato (o atributo local ficava mentindo `aria-expanded`) — os dois corrigidos. **2.241 testes** (eram 2.131), incluindo a primeira rede de regressão de `print:` do repositório. O roteiro C (seleção múltipla ponta a ponta com dados reais) **não foi executado** — sem Supabase local nem credencial de operador nesta máquina — e ficou pendência declarada. Decisões em `docs/DECISOES.md` (2026-08-09 · F30); evidências, os roteiros executados e "o que este relatório NÃO prova" em [`../RELATORIO-F30.md`](../RELATORIO-F30.md) |
| F31 | `F31-onda-c2-ultracode.md` | ✅ **CONCLUÍDA em 09/08/2026** (Onda C2, fecha o `BACKLOG-UX.md`). Dois recursos de itens por quantidade, **uma migration aditiva (`0104`) + zero dependência nova**: **ITN-01** dá "Transferir entre filiais" — a RPC transacional `transferir_item` grava **um par de ajustes** (`−N` na origem, `+N` no destino), porque o caminho intuitivo (Liberação+Entrada) **inflava o Total consolidado para sempre**; é `SECURITY INVOKER` (a permissão nas duas filiais é do Postgres, avaliada linha a linha) e fecha, ANTES de existir em produção, o mesmo tipo de deadlock que a `0100` (F24) só descobriu depois — travando as duas filiais em ordem determinística antes do primeiro insert; **18 asserções, 0 falhas nos dois bancos**. **ITN-04** entrega o Modo Conferência (`/itens/conferencia`): Sistema · Contado · Diferença ao vivo, "Registrar diferenças (N)" em blocos com progresso, linha em branco = "não conferi" (nunca zero), rascunho por filial em `sessionStorage`, sucesso parcial que reenvia só o que faltou. **Três voltas de revisão adversarial** — a segunda forçou um redesenho: a idempotência não podia depender de o `router.refresh()` já ter chegado (senão um clique de correção reenviava o ajuste inteiro), e a conta virou "base congelada na abertura + o que a própria sessão já gravou", verdadeira antes e depois do saldo novo chegar. Dois resíduos (escrita concorrente de outra pessoa no mesmo item; queda de rede sem chave de idempotência) ficaram **declarados**, não resolvidos — exigem servidor, fora do escopo da ordem. **2.343 testes** (eram 2.241) e a **34ª página de ajuda**. Migration aplicada pelo caminho A do runbook, ensaio e produção, contagens antes=depois nos dois. Decisões em `docs/DECISOES.md` (2026-08-09 · F31); evidências, os dois roteiros manuais com números antes/depois e "o que este relatório NÃO prova" em [`../RELATORIO-F31.md`](../RELATORIO-F31.md) |
| F32 | `F32-relatorios-v3-ultracode.md` | ✅ **CONCLUÍDA em 10/08/2026** (4 frentes A→B→C→D com `lint`+`test` verdes entre uma e outra; dentro de cada frente, agentes paralelos com dono exclusivo por arquivo, 16 agentes, 0 colisão). **24 itens (RV-01..RV-24)** do relatório v3 — fonte `docs/ANALISE-RELATORIOS-2026-08-10.md` —, **zero migration, zero dependência nova**: paleta de status corrigida por medição de daltonismo (ΔE 1,6 sob deutanopia entre manutenção e triagem — a triagem virou rosa `#db2777`), a mesma cor repetida em toda superfície (acento de 3px nos KPI tiles, swatch no glossário, gap de 2px entre marcas coladas), chip "foto × período", cards novos "Acervo por situação" e "Evolução do estoque" (as-of, sem RPC nova), clique-para-filtrar nos gráficos (só operador + ao vivo, mesmo gate dos KPI tiles), scroll-spy nos chips-âncora, medidor estoque×mínimo, e impressão P&B auditada canal a canal sem depender de cor. Dois defeitos **só apareceram rodando a página** — lint/tsc/build ficaram verdes nos dois: um ícone lucide passado como prop de Server Component para `'use client'` derrubava `/relatorios/[filial]` com HTTP 500, e `PREFIXO_FILTROS`, lido de um módulo cliente por um Server Component, valia `undefined` em silêncio (o clique-para-filtrar montaria `undefined.motivo=…`); os dois corrigidos e uma guarda nova (`fronteira-rsc.test.ts`) varre todo Server Component atrás de import de VALOR vindo de `'use client'`. Revisão adversarial: **5 achados, 3 confirmados** — entre eles um **byte NUL cru** que fazia o git classificar `transferencias-resumo.ts` como binário, escapando do próprio `git diff`/`blame`/`grep` que audita a fase. **2.505 testes** (eram 2.347). Decisões em `docs/DECISOES.md` (2026-08-10 · F32); evidências, os roteiros manuais e "o que este relatório NÃO prova" em [`../RELATORIO-F32.md`](../RELATORIO-F32.md) |
| F33 | `F33-performance-ultracode.md` | ✅ **CONCLUÍDA em 10/08/2026** (a ordem apontava o `getUser()` do proxy como suspeito nº 1; a medição o absolveu em 20 minutos — ~28ms de um TTFB de 1.200ms — e achou a causa real fora da ordem). **O sistema rodava no hemisfério errado:** `x-vercel-id` mostrava as funções renderizando em Washington (`iad1`) enquanto o Postgres está em São Paulo (`sa-east-1`), e cada navegação encadeia de 8 a 10 leituras sequenciais atravessando o continente uma vez por leitura; um `vercel.json` de três linhas (`regions: ["gru1"]`) resolveu: **−65% a −74% de TTFB** nas rotas de operador (meta da ordem era −30% nas três mais lentas; nenhuma rota piorou). Banco: **três migrations aditivas (`0105`–`0107`)** — índice da ordenação real de `/movimentacoes` (Seq Scan+Sort 68,6ms → Index Scan 0,11ms), 4 índices de FK em `movimentacoes`, e `e_admin()` dentro de `(select …)` numa policy (custo 164,50→89,76) —, aplicadas em ensaio e depois em produção. **Duas otimizações da própria ordem foram revertidas com o número que as condenou:** as "duplicatas" que o mapa apontara já valiam **1,00 query por render** dos dois lados, porque o Next já deduplica `fetch` idêntico dentro do mesmo render — só `getPerfilAtual()` (URL diferente, sem memoização) era real, e saiu de duas telas. Um índice funcional para `chave_identidade_ativo()` foi prototipado em 3 variantes e **nenhuma mudou o plano** — descartado com evidência, ficou pendência declarada (~72ms por navegação). Revisão adversarial achou um defeito que a própria fase introduzira: a promise de `listarFiliais` fora do `Promise.all` do motor do relatório podia derrubar o processo Node por rejeição não tratada, alcançando requisições concorrentes da mesma instância serverless — corrigido. Ferramenta nova versionada `scripts/perf/medir.mjs` (baseline/depois em `docs/perf/`, falha se alguma rota com sessão vier do cache de borda `HIT`). **2.517 testes** (eram 2.505). Deploy confirmado em produção (`bdb173d`, `READY`, `regions: ["gru1"]`). Decisões em `docs/DECISOES.md` (2026-08-10 · F33, oito atas); evidências em [`../RELATORIO-F33.md`](../RELATORIO-F33.md) |
| F34 | `F34-triagem-reserva-relatorio-ultracode.md` | ✅ **CONCLUÍDA em 11/08/2026** (orquestração multiagente: 6 leitores paralelos + crítico de completude → implementação em 6 frentes com dono exclusivo de arquivo → revisão adversarial de 7 lentes com cético final). Quatro mudanças pedidas pelo uso real. **A triagem virou opt-in:** `devolucao` (de `em_uso`/`emprestado`) passa a resultar **`em_estoque`** — antes toda devolução caía em `em_triagem` e exigia um `triagem_ok` só para o ativo voltar a existir como estoque — e nasce o tipo **`envio_triagem`** ("Envio para triagem", `em_estoque → em_triagem`) para quem quiser conferir; `triagem_ok` fica igual e **nenhum ativo muda de estado**. **A re-reserva:** o tipo `reserva` passa a valer sobre `reservado` (`reservado → reservado`), trocando colaborador/setor/chamado sem estorno e sem ajuste (o tipo `transferencia`, que é de **filial**, ficou byte a byte). **Relatório:** o texto copiado do "Resumo do período" perde o bloco "Em estoque (N)" (revogação parcial do item REL-08; a linha de totais fica byte a byte e o card visual continua) e os cards de manutenção passam a exibir chamado interno e do fornecedor em posição própria, sempre presentes, com "—" quando vazios. Duas migrations **aditivas** (`0108` valor de enum, `0109` as três funções por `create or replace` puro sobre o corpo **vigente lido do banco**, não sobre a migration antiga) e **nenhum `update` de dado**. Dois roteiros SQL teriam abortado no meio sem o conserto da fase (transição inválida fora de `begin/exception`, engolindo os cenários seguintes) e nasceu `f34_triagem_reserva.sql`; o job `banco` do CI rodou os **19 roteiros: 433 ✓, 0 ✗**. **2.512 testes** (eram 2.517 — 7 casos do bloco removido saíram, 2 novos entraram). Decisões em `docs/DECISOES.md` (2026-08-11 · F34); evidências, contagens por status antes = depois e "o que este relatório NÃO prova" em [`../RELATORIO-F34.md`](../RELATORIO-F34.md) |

| F35 | `F35-versionamento-credito-ultracode.md` | ✅ **CONCLUÍDA em 12/08/2026** (recon paralelo de 10 agentes — 5 fatias do `CHANGELOG.md` + inventário das ordens + 4 reconhecimentos de código — → `docs/PLAN-F35.md` com a tabela fase→versão inteira → 5 incrementos verificados → revisão adversarial). O sistema **passou a ter versão**: estava em produção desde 15/07/2026, com ~35 fases entregues, e o `package.json` ainda marcava o `0.1.0` do scaffold. O `CHANGELOG` inteiro virou semver — **fase = minor, entrega avulsa = patch, go-live de 15/07 = `1.0.0`**, pré-go-live em `0.x` —, dando **56 versões** de `0.1.0` a **`1.40.0`** (a ordem estimava "~1.29.0"; contado, o `1.29.0` caiu na F24). A fonte única é `src/lib/versoes/registry.ts`, no molde do registry da ajuda (F20): `VERSOES[0]` **é** a versão no ar, travada contra o `package.json` por teste. As `mudancas` são **traduzidas**, não copiadas — o CHANGELOG é narrativa de quem desenvolve, a tela fala com quem opera —, com guarda que recusa 21 termos de desenvolvedor. Tela nova **`/versoes`** (Server Component, só o registry, **zero consulta ao banco**, leitura de qualquer perfil ativo), **sem item na sidebar** por decisão da ordem: as portas são o badge `v1.40.0` no pé do menu, o `Ctrl+K` e a **35ª página de ajuda**. O badge não escreveu **uma linha de CSS** — reusa `data-sidebar-item`/`data-sidebar-rotulo` do colapso da F30, que ficou verde sem edição —, e o mesmo componente serve o `<aside>` do desktop e o Sheet do celular. **Crédito de autoria** em três pontos e só três (login, pé da sidebar, rodapé de `/versoes`), com `rel="noopener noreferrer"` e **nenhum recurso externo**; no login ele ficou **dentro do card** porque a medição reprovou o fundo: `muted-foreground` sobre `bg-muted` dá **4,34:1** no tema claro, abaixo do piso AA. E virou **processo**: o item 8 do `CLAUDE.md` exige bump + entrada no registry + tag ao fim de toda ordem, com `cobertura-changelog.test.ts` derrubando o `npm run test` se uma entrada nova do CHANGELOG ficar sem versão. **Zero migration, zero dependência nova** (`git diff supabase/` vazio; no `package.json`, só `version`). **2.544 testes** (eram 2.523); primeira tag do repositório, `v1.40.0`. Decisões em `docs/DECISOES.md` (2026-08-12 · F35); a tabela fase→versão e "o que este relatório NÃO prova" em [`../RELATORIO-F35.md`](../RELATORIO-F35.md) |
| F36 | *(sem ordem própria — ver o relatório)* | ✅ **CONCLUÍDA em 28/08/2026** (**v1.41.0**). A regra de "quem está com o equipamento" existia em **três cópias divergentes** — seis tipos de movimentação em `aplicar_movimentacao`, cinco em `rel_estoque_asof` e uma lista de estados escrita à mão dentro do "Forçar estado". O furo principal era o **`ajuste`**, que gravava o estado direto e não limpava nada: um equipamento chegava a "Em estoque com o Fulano". As três listas viraram **uma pergunta ao estado resultante** (`status_tem_detentor`). Duas migrations (`0110` recria quatro funções por `create or replace` puro sobre o corpo lido do banco; `0111` limpa 4 ativos legados). **É retroativo e foi contado antes:** 21 movimentações de julho/agosto mudam de leitura no relatório as-of. ⚠ **Esta fase não deixou arquivo de ordem nesta pasta** — a régua dela é o §3 do `docs/PLAN-F36-F39.md`, e as evidências, o checklist e "o que este relatório NÃO prova" estão em [`../RELATORIO-F36.md`](../RELATORIO-F36.md) |
| F37 | `F37-fundacao-colaboradores-tipos-ultracode.md` | ✅ **CONCLUÍDA em 28/08/2026** (**v1.42.0**; 8 leitores paralelos → `PLAN.md` → incrementos verificados → revisão adversarial de 6 lentes com cético). Fase de **fundação**, e a régua foi o que **não** mudou. Três migrations que **só criam**: `0112` (a função IMMUTABLE `colaborador_chave`, a tabela `colaboradores` com chave de deduplicação **gerada** + índice único, e a view da fila de consolidação), `0113` (as colunas `colaborador_id` **anuláveis** em `movimentacoes` e `lancamentos_item`) e `0114` (`tipos_item` com os **7 slugs que o histórico já guardava** + `itens.tipo_id` anulável). **Nenhuma função ou trigger existente recriado, nenhum registro histórico alterado** — e isso não é promessa: `guarda_acervo` recusa UPDATE em histórico a todo mundo, service role incluso, e o roteiro novo prova por **asserção negativa rodando como o DONO**. Daí o desenho: a ligação do passado é **por chave na leitura**, e a tela de consolidação **cria cadastro** em vez de reescrever movimentação. O vínculo do registro NOVO é resolvido **no servidor**, pela chave normalizada do próprio texto — **nenhum id viaja pelo formulário**, e por isso rascunho, "repetir última", kits e resumo de revisão ficaram idênticos, **sem uma linha de teste existente editada**. A equivalência TS↔SQL da normalização virou prova: a expressão usa **classe de espaço explícita** porque o `s` do Postgres e o do JavaScript **não são o mesmo conjunto** (NBSP). Duas telas novas (`/admin/colaboradores`, `/admin/tipos-item`), coluna de tipo em `/admin/itens`, e `fone` passando a exibir "Fone de ouvido" nos dois lados. **Da medição (D6) saiu o harness escrito e guardado, mas a curva dos três patamares NÃO foi levantada** — o projeto de ensaio está pausado e produção é proibida para ele: é a **pendência número um**, com a âncora do volume de hoje medida só-leitura no lugar. **Zero otimização entrou.** **2.646 testes** (eram 2.609); 19 dos 21 roteiros SQL rodados contra o banco real, **0 falhas** (o roteiro novo com 24 asserções, o `papeis_rls.sql` com 74). Decisões em `docs/DECISOES.md` (2026-08-28 · F37); evidências e "o que este relatório NÃO prova" em [`../RELATORIO-F37.md`](../RELATORIO-F37.md) |
| F38 | `F38-itens-andam-com-o-ativo-ultracode.md` | ✅ **CONCLUÍDA em 28/08/2026** (**v1.43.0**; exploração com subagentes paralelos → `PLAN.md` autossuficiente com o DDL final antes de implementar → incrementos verificados nesta ordem — curva → vínculo → RPC do lote → saldo por pessoa → checklist → ciclo da pendência → revisão adversarial final). O acessório e o equipamento viviam em dois mundos que não se falavam: `lancamentos_item` não tinha elo com `movimentacoes`, não existia conta por pessoa, o checklist de devolução eram sete códigos fixos que não tocavam o estoque, e o lote de movimentação gravava linha a linha. **O vínculo** (`0116`, `lancamentos_item.movimentacao_id`, FK imediata anulável) fez "o que foi junto com este notebook" virar join. **O lote virou tudo-ou-nada** (`0117`, RPC `criar_movimentacao_com_itens`, security invoker) — uma linha ruim derruba o lote inteiro (decisão do Johnny, 28/08/2026), e a leitura mostrou que o "sucesso parcial" nunca passava pelo `painel-sucesso.tsx`: o caminho real era `nova-movimentacao-form.tsx`, reescrito por inteiro, com `jaRegistrados` removido. A RPC trava **duas classes de lock** em ordem determinística antes do primeiro INSERT: as advisory locks nomeadas pela ordem, mais os `select … for update` por ativo que numa transação só passam a coexistir. **A conta por pessoa** (`0118`, RPC `rel_saldo_colaborador`) é uma partição de `liberados` (cabeçalho da `0027`), com guarda nova em `valida_lancamento_item` que recusa `retorno` acima do saldo — **nenhum número da tela de itens mudou**, conferido linha a linha em produção antes/depois do apply. **O ciclo da pendência fecha** (`0119`, `pendencia_item_id`): "Item recuperado" grava um `retorno`; "Baixa" grava `retorno` **+** `ajuste` negativo — dois lançamentos, porque `ajuste` sozinho deixaria o item na conta da pessoa para sempre. **O estorno desfaz o conjunto, ou recusa** (`0121`). A curva de desempenho — pendência nº 1 da F37 — foi finalmente levantada: a primeira corrida (trigger ligado) foi interrompida em 126 mil linhas, um lote de 2.000 passando de ~6 s para ~120 s — o próprio achado que a ordem previa; a segunda mediu o custo real por INSERT do trigger, que saiu **plano** (3,3 ms a 10 mil, 3,5 ms a 500 mil) contra o driver quadrático que a leitura de código previa, porque `lanc_item_item_filial_idx` já serve a agregação. O índice novo (`0120`, `lanc_item_colaborador_idx`) entrou **com o número na mão**: 111,56 ms → 9,22 ms (12,1×) aos 500 mil, custo de +0,19 ms por escrita — migration a mais do que a ordem nomeou, porque dependia da curva; `0121` (estorno acoplado) também é migration a mais, por precisar de RPC própria. **Seis migrations (`0116`–`0121`)** — a ordem previa quatro e numerava o vínculo como `0115`, que a revisão de código da F37 (v1.42.1) já tinha usado. Achado de passagem, de OUTRA fase: dez roteiros escolhiam o perfil-autor com `select … limit 1` sem filtro nem ordem, caindo num perfil desativado e quebrando dois roteiros que passavam verdes no dia anterior — corrigido nos dez. Roteiro novo `f38_itens_com_ativo.sql`, **44 asserções, 0 falhas**; as dez funções que a fase promete não tocar saíram idênticas por md5, antes e depois do apply em produção. **Zero dependência nova.** Decisões em `docs/DECISOES.md` (2026-08-28 · F38); evidências, a curva completa e "o que este relatório NÃO prova" em [`../RELATORIO-F38.md`](../RELATORIO-F38.md) |
| F39 | `F39-termo-diz-o-que-foi-junto-ultracode.md` | ✅ **CONCLUÍDA em 29/08/2026** (**v1.44.0**; fecha a série F36→F39). O termo mentia por omissão: a F38 fez o acessório andar junto com o equipamento no banco, mas o papel que a pessoa assina listava só marca, modelo, service tag e patrimônio. **Os 5 modelos `.docx` de responsabilidade ganharam a seção de acessórios** — por **script** (`scripts/termos/inserir-acessorios.mjs`, molde do `retaguear-cidade.mjs` da F25), nunca pelo Word, porque reabrir e salvar reescreve o pacote e nenhuma inspeção prova depois que só a seção nova mudou. A cláusula aprovada entra em **três parágrafos** (bloco condicional com as tags sozinhas), que é a forma que o `paragraphLoop` remove por inteiro sem periférico — conferido na doc oficial vigente do docxtemplater, não de memória. O `w:pPr` do vizinho é clonado **menos o `w:numPr`** (a cláusula é frase, não item da lista de identificação). Prova por modelo: **5** arquivos mudados (não 7), **+3** `<w:p>`, uma única parte divergente, XML anterior reconstituível, foro e assinatura intactos — e o **critério 2 saiu melhor que o exigido**: renderizado SEM periférico, o documento sai **byte a byte** igual ao da baseline nos cinco. **A linha vem pronta** de `src/lib/termos/acessorios.ts` (função pura, 19 testes): agrupa por tipo, soma ("Mouse (2)"), ordena pela ordem do catálogo, descarta item sem tipo **contando quantos** (é o aviso) e corta com " e mais N" no teto do campo, em vez de deixar `gerarTermo` morrer num "Há campos inválidos". **A devolução (D11)**: `{outros_componentes}` deixou de ser `''` fixo desde a F5A e passou a listar o que voltou; `{observacao}` continua dizendo o que faltou. Duas exclusões de correção (`estorna_id`, `pendencia_item_id`) com filtro no SQL **e** recusa na função pura. `tem_acessorios` deriva do **texto final**, no servidor — apagar o campo faz a seção sumir do papel, digitar à mão faz aparecer, conferido nos dois sentidos no arquivo gerado. **A constante `ACESSORIOS_DEVOLUCAO` saiu do código** (pendência nº 4 da F38): sete superfícies migradas para `tipos_item`, sempre por prop a partir de um Server Component, com o relatório lendo pelo **client resolvido** (senão o visualizador por senha veria slug cru) — **nenhum rótulo mudou**, e a guarda TS↔SQL foi **invertida** (passou a proteger os sete slugs históricos no seed da `0114`) em vez de apagada. **Um defeito que só o navegador pegou**: o mapa de rótulos nascia com protótipo nulo e o React recusa isso como prop de Client Component — o render no servidor do relatório caía; nem o `build` nem os 2.825 testes viam. **Zero migration, zero dependência nova** (`git diff supabase/` vazio). ⚠ Em produção, `lancamentos_item.movimentacao_id` está preenchido em **0** linhas e **7 dos 18** itens têm tipo: nenhuma movimentação existente produz linha de acessório hoje — a fase se prova no ensaio e nos testes. Evidências para a conferência visual do Johnny em `docs/f39-evidencias/`; checklist, decisões e "o que este relatório NÃO prova" em [`../RELATORIO-F39.md`](../RELATORIO-F39.md) |

> **Colisão de nome.** Esta é a **segunda** ordem chamada `F20-ultracode.md` (a primeira, de 24/07, virou a `/ajuda` multi-página). A de 28/07 foi arquivada como **`F20B-ultracode.md`** e o relatório dela como `RELATORIO-F20B.md`, para não sobrescrever os da anterior — mesmo precedente da F19.

> **Refinos deploy-only (sem ordem de serviço própria).** As levas **F7G** (`.xlsx` nativo), **F7-pós** (hostname automático + ausência textual ampliada), **F7J** (hostname curto + forçar patrimônio, migration 0037) e **F7K** (modelo que repete a marca) foram melhorias do import aplicadas direto em 20/07/2026, registradas no [`CHANGELOG.md`](../../CHANGELOG.md) e em `docs/DECISOES.md`. Não têm arquivo `F*.md` porque não foram executadas como ordem de serviço formal.
