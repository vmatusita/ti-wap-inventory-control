# F62 — as sabotagens A a J, cada uma com a saída real

Índice das provas obrigatórias da ordem ("Verificação — rode de verdade"). Cada linha abaixo é **copiada** de um
arquivo desta pasta, e o arquivo é a saída real do comando — nada aqui é resumo do que "deveria" acontecer. CI: run
[`35783705125`](https://github.com/vmatusita/ti-wap-inventory-control/actions/runs/35783705125) sobre o SHA de código
congelado `f31175a`. Dado 100% fictício (fixtures do CI e da mesa).

| sabotagem | o que se sabota | a prova vermelha (real) | o verde | arquivo |
|---|---|---|---|---|
| **A — a varredura do cargo** | as quatro travas contra o código de ANTES; uma função, um `from('profiles').select('papel')` e um roteiro sintéticos | `× nenhuma função vigente fora de k_excecoes_cargo lê ou escreve profiles.papel/profiles.ativo` — "apagar_usuario (forma b, vigente em 0074…) · checagens_integridade_nucleo … · definir_papel_usuario …" (mesa); no catálogo do CI, `✗ 1a … acusadas: apagar_usuario (b), checagens_integridade_nucleo (b), definir_papel_usuario (b), definir_status_usuario (b), definir_vinculos_usuario (b), exigir_gestao_de (b), existe_outro_admin_ativo (b), papel_atual (b): 8 de 94` — as oito que tinham de mudar (a nona leitora do fato 6, `profiles_guarda_dev`, é a exceção nomeada); TS e roteiros com os leitores e os 18 roteiros | `cargo_em_membros.sql` 1a–1d (0 de 102 funções, 0 de 9 views, 0 de 62 policies) e 2 (auto-sabotagem acusa as quatro fictícias); mesa describes 3, 5 e 7 ("a varredura SABE reprovar") | `B-travas/mesa-ts-roteiros-vermelho.txt`, `B-travas/catalogo-vermelho-ci.txt`, `C-ci-verde.txt`, `E-mesa-verde.txt` |
| **B — a comparação** | o corpo novo de `papel_atual()` sem o `excluido_em` e, depois, sem o `ativo` | `✗ 1 papel_atual(): antigo = vivo em toda a grade — [dev\|ativo\|arquivado\|sem vinculo: antigo=∅ vivo=dev] …` e `… [dev\|inativo\|vivo\|sem vinculo: antigo=∅ vivo=dev] …` — a célula da grade NOMEADA | `cargo_equivalencia.sql` 1–5, 4b e 6 verdes | `D-mutacoes-f62-no-ci.txt` (mutações 111 e 112), `C-ci-verde.txt` |
| **C — o isolamento** | `empresas_do_membro()` sem `m.ativo`; `unidades_de_escrita()` sem `f.empresa_id = m.empresa_id`; a forma `exists (select 1 from public.membros …)` numa policy sintética | `✗ 9e a desativação não valeu no statement seguinte …`; `✗ 9d unidades_de_escrita() nunca devolve par de filial de outra empresa … admin_a(6 pares, 1 fora de A) consultor(6 pares, 1 fora de A)`; mesa: `R3 · exists que lê tabela sem olhar a linha → reprova` | 9e e 9d verdes | `D-mutacoes-f62-no-ci.txt` (106, 109), `E-mesa-verde.txt` (`policies-initplan.test.ts`) |
| **D — o `force`** | `alter table public.membros force row level security` | `✗ 4-bis force row level security desligado em toda tabela de public (R-ACC-29) — ligado em: membros …: 1 de 28 fora da regra` | 4-bis (0 de 28); **e o `42P17` previsto NÃO acontece**: `9o (medição) com force em membros, admin_a leu papel/linhas = admin/9` — o dono tem BYPASSRLS/superusuário (medido no ensaio: `postgres` `rolbypassrls = true`). A premissa real virou asserção (9o, 0 de 17) | `D-mutacoes-f62-no-ci.txt` (124), `C-ci-verde.txt` |
| **E — a guarda do dev** | `update public.membros set papel = 'dev'` sem a janela; a guarda neutralizada por mutação | `✗ 2h-membros SERVICE ROLE concedeu o cargo dev por UPDATE direto em membros` (e 2i-membros, 2j-membros…) com a guarda neutralizada | `✓ 2h-membros service role recusado (42501) ao conceder dev em membros` | `D-mutacoes-f62-no-ci.txt` (117), `C-ci-verde.txt` |
| **F — a revogação** | o membro desativado, na mesma sessão, sem token novo | (a mutação da C derruba o mesmo cenário) | `✓ 9e o membro desativado some no statement seguinte, sem token novo (antes: A e operador; depois: nenhuma empresa, sem cargo, sem unidade)` | `C-ci-verde.txt` |
| **G — o rollback ensaiado** | mudar cargo e desativar DEPOIS das migrations; rollback na ordem escrita; e o mesmo SEM a cópia | `✓ rb2 SEM a cópia, o rollback devolve o estado VELHO — o desligado voltaria a ter cargo (operador/operador/operador) e a impressão muda` | `✓ rb1 rollback na ordem escrita (cópia primeiro): o corpo antigo devolve o estado MAIS NOVO … e a impressão de todo perfil é a mesma de antes do rollback` | `C-ci-verde.txt` (`f62_rollback.sql`) |
| **H — os slugs** | slug `admin`, `todas`, com maiúscula; um segmento de rota de topo sintético não reservado | mesa: `SABOTAGEM H (mesa): uma rota de topo sintética não reservada é acusada` | `✓ 9m o slug reservado ou fora do formato é recusado pelo CHECK (admin, todas, relatorios, maiúscula, espaço, hífen na ponta), e o legítimo passa` | `E-mesa-verde.txt`, `C-ci-verde.txt` |
| **I — o `config`** | `'"texto"'::jsonb` e `'[]'::jsonb` | (recusa pelo CHECK, dentro do cenário) | `✓ 9n empresas.config recusa jsonb que não seja objeto ("texto", [], 42, null) e aceita objeto` | `C-ci-verde.txt` |
| **J — o usuário novo** | conta fictícia em `auth.users`; o `handle_new_user` sem a linha da membership, por mutação | `✗ 9l toda conta nova nasce com a membership operador ATIVA na empresa legada (handle_new_user): 6 de 6 fora da regra` | `✓ 9l … (0 de 6 conferidos)` | `D-mutacoes-f62-no-ci.txt` (118), `C-ci-verde.txt` |

**Além das dez** (achados da revisão adversarial, cada um com prova própria): a RPC antiga em voo no apply —
`✗ 8d a escrita da RPC antiga (janela aberta, sem a marca) gravou em silêncio na coluna congelada` com a mutação
`f62-guarda-de-profiles-aceita-a-escrita-antiga`, e o verde `✓ 8d a RPC antiga em voo é recusada (55000) …`; a
recópia da 0158 — `✓ 6 a recópia da 0158 reconcilia promovido, desligado e perfil sem membership, sem reescrever as
linhas que já batiam nem tocar outra empresa`, com a trava de mesa 8-bis e a sua sabotagem (o `where` do upsert
trocado é acusado).
