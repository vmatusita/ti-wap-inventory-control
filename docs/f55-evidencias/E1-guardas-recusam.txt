SABOTAGEM F — AS GUARDAS DE AMBIENTE RECUSAM (F55 · Frente E · 10/09/2026)
As guardas rodam ANTES de qualquer rede. Nenhum banco foi tocado em nenhuma das provas.

===========================================================================
1) db:reset com um ref INVENTADO (variaveis sobrepostas no ambiente do comando)
===========================================================================



[GUARDA] Execucao recusada:
- O ref "zzzzinventadozzzzzzzz" nao esta na lista de ensaio permitida (REFS_DE_ENSAIO em scripts/env-guard.ts). Aponte NEXT_PUBLIC_SUPABASE_URL e SEED_PROJECT_REF para o projeto de ensaio antes de rodar seed/reset.

   exit=1

===========================================================================
2) db:reset com o ref de PRODUCAO
===========================================================================



[GUARDA] Execucao recusada:
- O ref "pbtjcalbmepmrqzprusb" e PRODUCAO (docs/RUNBOOK-BANCO.md). Dados ficticios nunca entram nela. Aponte NEXT_PUBLIC_SUPABASE_URL e SEED_PROJECT_REF para o projeto de ensaio antes de rodar seed/reset.

   exit=1

===========================================================================
3) ref do ENSAIO, mas o BANCO nao confirma o rotulo  (resposta SIMULADA)
===========================================================================
   ⚠ A resposta do banco e SIMULADA: o ensaio de verdade responde
     'desenvolvimento', e apagar a linha de public.ambiente la para provar o
     contrario seria mexer num banco que outra gente usa. O que se prova
     aqui e a GUARDA — dado um banco que nao confirma, ela recusa.

--- caso 0: banco de PRODUCAO (a tabela `ambiente` e vazia la): rotulo = null ---

[GUARDA] Execucao recusada:
- O banco nao se identifica como ambiente de desenvolvimento (rotulo_de_ambiente() = null). Dados ficticios nunca entram num banco que nao confirma ser o ensaio.

   exit=1

--- caso 1: banco que responde outra coisa ---

[GUARDA] Execucao recusada:
- O banco nao se identifica como ambiente de desenvolvimento (rotulo_de_ambiente() = "producao"). Dados ficticios nunca entram num banco que nao confirma ser o ensaio.

   exit=1

--- caso 2: a RPC falhou (funcao ausente, rede caiu) ---

[GUARDA] Execucao recusada:
- Nao consegui confirmar o ambiente pelo banco (public.rotulo_de_ambiente() falhou: function public.rotulo_de_ambiente() does not exist). Falha na checagem e recusa (falha fechada) — o script nao prossegue sem a confirmacao do proprio banco.

   exit=1

===========================================================================
===========================================================================
4) restaurar.mjs com ref INVENTADO
===========================================================================

[GUARDA] Execução recusada: o ref "zzzzinventadozzzzzzzz" não está na lista de ensaio
permitida (REFS_DE_ENSAIO em scripts/db/restaurar.mjs). Aponte DATABASE_URL e
NEXT_PUBLIC_SUPABASE_URL para o projeto de ensaio, ou para um Postgres
descartável sem ref (como no CI).

   exit=1

===========================================================================
5) restaurar.mjs com ref de PRODUCAO
===========================================================================

[GUARDA] Execução recusada: o ref "pbtjcalbmepmrqzprusb" é PRODUÇÃO (docs/RUNBOOK-BANCO.md).
Este script NUNCA restaura em produção — nem com confirmação, nem com variável
de ambiente. Aponte DATABASE_URL e NEXT_PUBLIC_SUPABASE_URL para o projeto de
ensaio, ou para um Postgres descartável.

   exit=1

===========================================================================
6) restaurar.mjs ACEITA Postgres LOCAL sem ref (o caso do CI)
===========================================================================

[restaurar] Para ler do bucket é preciso NEXT_PUBLIC_SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY.
   exit=1
   ⚠ A GUARDA DEIXOU PASSAR (e o certo: o CI restaura num Postgres local sem ref).
     O erro acima e de ARQUIVO/CONEXAO, nao de guarda — esta mesa nao tem Postgres.
