# Controle de Estoque TI — WAP

Sistema interno para substituir o controle de ativos de TI feito hoje em três planilhas desconectadas (inventário, saída e devolução) + revisão por Gmail + relatório semanal manual por filial.

**Ideia central:** registra-se só a *movimentação* (saída, devolução, compra, transferência…) uma única vez; o estado do ativo, o estoque e os relatórios por filial derivam automaticamente. Relatório deixa de ser enviado — vira um link sempre atualizado.

## Stack (decidida em 09/07/2026)

- **Next.js 16** (App Router, Turbopack) + TypeScript
- **Tailwind CSS** + **shadcn/ui** + Recharts v3 (análise de alternativas no PLANEJAMENTO §2.1)
- **Supabase** (plano Free) — Postgres, Auth (login por convite), RLS, Realtime
- Deploy: **Vercel** (conta Pro já paga — custo novo R$ 0 para a empresa)

**Regra de patrimônio:** o patrimônio identifica o equipamento, mas repete em casos raros — quem é único é o par **patrimônio + service tag** (spec §5, regra 1).

**Modelo de acesso:** operar = login com conta WAP (só `@wap.ind.br`, nível único — todo logado é admin); visualizar relatórios = **senha de acesso, sem conta** (senhas com rótulo, criadas e revogadas individualmente pelo admin) — spec §3.

## Estrutura deste repositório

| Caminho | Conteúdo |
|---|---|
| `CLAUDE.md` | Regras permanentes para o Claude Code (lido automaticamente em toda sessão): modo autônomo, stack travada, convenções, estrutura prescrita, dados fictícios, custo zero |
| `docs/ESPECIFICACAO.md` | **O quê** — especificação completa: problema, conceito, modelo de dados, telas, regras, fases e perguntas em aberto |
| `docs/PLANEJAMENTO.md` | **Como e quando** — stack fechada, estratégia de dados, fases com critérios e o fluxo de execução via Claude Code (§9) |
| `docs/prompts/` | **Ordens de serviço F0–F5** — um prompt detalhado por fase, pronto para colar no Claude Code (leia `docs/prompts/README.md` antes) |
| `docs/DECISOES.md` | Rastro de auditoria das decisões autônomas |
| `supabase/migrations/` | Fonte da verdade do banco a partir da F1 (`schema.sql` é histórico) |
| `mockups/dashboard-relatorio.html` | Mockup navegável do relatório por filial, com os números reais das planilhas de 2026 |

## Desenvolvimento local

Pré-requisitos: **Node 20+** e **npm**. Banco, Auth e Storage rodam num projeto **Supabase de desenvolvimento** — nunca produção.

1. `npm install`
2. Copie `.env.example` para `.env.local` e preencha com os valores do Supabase **de DEV**.
3. `npm run dev` → app em `http://localhost:3000`.

Variáveis de ambiente (todas documentadas em `.env.example`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (só server-side/scripts — nunca `NEXT_PUBLIC`), `VIEW_SESSION_SECRET` (assina o cookie da sessão de visualização por senha) e as guardas `SEED_CONFIRM`/`SEED_PROJECT_REF`, que impedem os scripts de dados fictícios de rodarem fora do DEV.

Scripts (dados 100% fictícios — jamais apontam para produção):

| Comando | O quê faz |
|---|---|
| `npm run dev` / `npm run build` / `npm run lint` | Desenvolvimento · build de produção · verificação obrigatória ao fim de cada ordem |
| `npm run db:seed` | Popula o banco de DEV com dados fictícios determinísticos |
| `npm run db:reset` | Zera as tabelas de dados (exige as guardas do `.env.local`) |
| `npm run db:types` | Regenera `src/lib/types/database.ts` a partir do schema (CLI linkada; grava só se a saída for TypeScript válido) |

## Status

- [x] Análise das planilhas reais (1.179 ativos, 423 saídas, 291 devoluções — jan–jul/2026) e dos e-mails semanais
- [x] Decisões de arquitetura confirmadas
- [x] Especificação v1.1
- [x] Planejamento de desenvolvimento (stack fechada, fases F0–F5, estratégia de dados)
- [x] Ordens de serviço para o Claude Code (`CLAUDE.md` + `docs/prompts/F0–F5`)
- [x] Planejamento validado → execução autônoma iniciada
- [x] **F0 — fundação** — concluída em 10/07/2026 (Next 16.2.10, login por convite restrito a `@wap.ind.br`, layout, sessão via proxy, deploy)
- [x] **F1 — banco + dados fictícios** — concluída em 10/07/2026 (migrations 0001–0007 incl. `senhas_acesso`, seed determinístico com guardas anti-produção, tipos gerados, roteiro SQL de teste da máquina de estados)
- [x] **F2 — operação** — concluída em 13/07/2026 (lista com filtros/busca server-side + data-table, ficha com linha do tempo, nova movimentação em lote com validações Zod espelhando a máquina de estados, estorno da última movimentação, **entrada de equipamento novo por compra — single/lote via lista ou faixa de patrimônios, atômica (RPC `criar_compra_lote`, migration 0008)**, facilitadores anti-Excel: atalho `N`, "repetir última", "duplicar", data default, foco na busca)
- [x] **F3 — relatórios + administração** — concluída em 13/07/2026 (relatório **ao vivo** `/relatorios/[filial]` + consolidado `geral` com tabs, filtro de período, KPIs, gráficos Recharts via wrapper `chart`, disponíveis por modelo, em manutenção, reservados, últimas movimentações com observação, **resumo no formato do e-mail**; **snapshot semanal versionado** `/relatorios/gerados` — imutável, "fim da errata"; **acesso por senha** sem conta — scrypt + cookie HMAC, revogação com efeito no request seguinte, shell reduzido; **tempo real** p/ operador + auto-refresh p/ visualizador; export CSV `;`+BOM e impressão; administração: convites `@wap.ind.br`, senhas de acesso, filiais, motivos; migrations 0009–0011)
- [x] **F3B — relatórios v2** — concluída em 14/07/2026 (relatório no **formato do e-mail**: 3 grupos — principais/acessórios/componentes — + tabelas de Saídas/Entradas/Transferências, chips-âncora, KPIs com Δ; **itens por quantidade** antecipados da F5 — catálogo `admin/itens` + tela `/itens` com saldo/atrelados/**falta** automática, trigger de saldo no Postgres; **anotações** na linha do tempo; reconstrução **as-of** do estoque; snapshot **schema 2** que congela os 3 grupos e mantém v1 abrindo; **sem export CSV**; migrations 0014–0018 — plano `docs/PLANO-RELATORIOS-V2.md`, resumo aprovado `docs/RELATORIO-V2-PARA-APROVACAO.md`)
- [x] **F5A — termos gerados pelo sistema** — concluída em 14/07/2026 (os 7 modelos — 5 de responsabilidade + 2 de devolução — gerados em `.docx` **fiel ao original**, preenchidos pelo sistema com todos os campos editáveis, **preview do arquivo real** no navegador e download; snapshot `jsonb` + arquivo no **Storage privado**, versão única por termo; novo status `gerado` que continua contando como pendência; migrations 0020–0021; plano `docs/PLANO-TERMOS.md`, OS `docs/prompts/F5A-termos.md`). Independente da F4 — roda antes ou depois do go-live.
- [x] **F4 — carga inicial única + go-live** — **CUTOVER em 15/07/2026**: dados reais em produção (**1.596 ativos** — 17 inferidos — e **3.231 movimentações** das 5 filiais, zero falhas do trigger; estado conferido 1.596/1.596 contra a planilha; reexecução idempotente sem duplicar nada). Scripts em `scripts/import/` (3 layouts por nome de coluna, reconciliação compra→replay→ajuste, dry-run com relatório de inconsistências, guardas `CARGA_*`), revisão adversarial com 8 correções, ensaio aprovado no 2º projeto free antes da produção; migration 0026 (filial `serra`); ata completa em `docs/DECISOES.md`. **A partir de agora as planilhas antigas são somente-leitura e não existe importação — a entrada de dados é 100% pelo sistema.** Pendente da janela: carga dos **saldos de itens** (aguarda o export da planilha de gestão online) e criação das senhas de acesso das filiais em `admin/senhas`
- [x] **F6A — correções pós-go-live** — concluída em 16/07/2026 (execução multi-agente `F6A-ultracode.md`): filtro da carga go-live fora do relatório do período (`.or` null-safe, sem tocar dado); semântica **Total/Estoque** dos itens (atrelar/liberar descontam o estoque, devolução/retorno repõem — migration 0027, novo tipo `retorno`, estorno redesenhado, `falta` = déficit real); **pendências só para o operador** com página `/pendencias` (migration 0028, corte no render + defesa em profundidade). Migrations aplicadas em produção, deploy único, revisão adversarial + auditoria A2. Pendência devolvida ao Johnny: 2 snapshots congelados do go-live com a carga (chip). Próximo: F6B
- [x] **F6B — melhorias de UX** — concluída em 16/07/2026 (execução multi-agente `F6B-ultracode.md`, 2 ondas em worktrees isolados + revisão adversarial): loading nativo (barra global + skeletons por rota, zero lib); relatório ao vivo default **semana dom–sáb**; resumo com quebra de linha no `;`; **observação** no snapshot (migration 0030) visível ao visualizador; **seção de movimentações de itens** no relatório (exclui a carga F6C); **confirmar/desfazer assinatura** do termo (rastro em anotações); **corrigir patrimônio** (service tag imutável); **sessões de 24h** (operador + visualizador); **página `/ajuda`** derivada de `dominio.ts`. Migration 0030 em produção, deploy único, zero dependência nova. Decisões em `docs/DECISOES.md` (2026-07-16 · F6B). Próximo: F6C
- [x] **F7 — import de startup por CSV no admin** — construída, integrada, revisada e documentada na `main` em 16/07/2026 (execução multi-agente `F7-ultracode.md`: W1 motor ∥ W2 banco → W3 tela → revisão adversarial de 7 dimensões, 1 achado corrigido — janela TOCTOU fechada com re-checagem de contagens sob o lock) e **em produção — gate resolvido, confirmado em 17/07/2026** (`import_logs` e a RPC `importar_ativos_substituir` no ar; o gate anterior era o classificador do modo automático, que bloqueia a *execução* do import destrutivo). Tela `admin/importar` para o **go-live novo de cada filial** — só modo Substituir tudo (delete físico assumido, com backup automático + preview do custo + confirmação pelo nome da filial), preview tudo-ou-nada com erros linha a linha, **entrada com a data real do CSV** (sem data válida = marcada fora dos relatórios do período). Modo Atualizar adiado. Migrations `0031` (import_logs + bucket) e `0032` (RPC transacional). **Revoga a regra "não existe importação, nunca"** — documentos emendados (spec §10.2, CLAUDE.md, este README). `lint`+`test`(333)+`build` verdes. Decisões em `docs/DECISOES.md` (2026-07-16 · F7). Correção de erros na tela: **F7B**
- [x] **F7B — correção de erros do import na tela** — concluída em 17/07/2026 (execução multi-agente `F7B-ultracode.md`: W1 motor ∥ W2 banco → W3 validators+actions+UI → revisão adversarial + E2E em DEV + emendas). No passo Preview do `admin/importar`, todo erro/aviso vira **correção dentro do sistema**: erros repetidos **agrupados pelo valor cru** e corrigidos **em massa** com 1 clique (categoria, estado, site desconhecido, data — sugestão pré-selecionada por Levenshtein próprio quando o valor é um typo próximo), pontuais **com o contexto da linha** (patrimônio com preview da canonicalização, service tag, colaborador), remoção de linha, painel de **correções aplicadas com Desfazer** ("sem efeito" quando a op não casa) e **reanálise total automática** pelo motor a cada mudança — a régua da F7 não mudou, mudou a forma de alimentá-la. Decisões do Johnny (17/07): correção na tela com facilitadores e em massa; **bloqueantes E avisos** corrigíveis; correções valem **só no import atual** (sem catálogo persistente); **site de outra filial conhecida só remove a linha** (transferência é operação do sistema). O **CSV original nunca muda** (`arquivoHash` continua sendo o dele) — as correções são ops auditadas em `import_logs.correcoes` (migration `0033`: coluna + RPC recriada com `p_correcoes`), o passo 5 e o histórico mostram a contagem, e "Baixar CSV corrigido" gera o arquivo do que foi importado (reimporta limpo). Invariantes da F7 intactas (tudo-ou-nada, backup, confirmação pelo nome da filial, contagens TOCTOU, advisory lock, RLS; descartado segue bloqueante) — a revisão adversarial provou o import sem correções **idêntico à F7** (26/26 comparações contra o commit da F7) e corrigiu 5 achados (o mais grave: a decisão 4 era furável pela via pontual `editar`+site). Zero dependência nova. `lint`+`test`(447)+`build` verdes. Decisões em `docs/DECISOES.md` (2026-07-17 · F7B). **Em produção em 17/07/2026**: migration `0033` aplicada pelo Johnny (o classificador do modo automático bloqueia a DDL da função destrutiva em produção — mesmo gate da F7), conferida antes do merge (função única com 4 args, sem overload; `anon` sem execute), deploy único READY. Smoke de produção (só leitura): PostgREST enxerga `import_logs.correcoes` e resolve a RPC de 4 args, recusando `anon` com `42501` — a invariante do grant de pé
- [x] **F7E — import: datas `dd/MMM`, patrimônio vazio como pendência, cards agrupados** — concluída e **em produção** em 17/07/2026 (execução multi-agente `F7E-ultracode.md`: W1 motor ∥ W2 banco → integração → W3 UI/actions ∥ W4 pendência fora do import → **revisão adversarial de 5 dimensões, 0 defeitos confirmados** + E2E completo em DEV). Três dores do 1º CSV real da Matriz, sem afrouxar a régua nem as salvaguardas: **datas de entrega `dd/MMM`** (`18/nov`, `21/jan`) puxam o ano da Data de Inclusão (+1 na virada) e datam o **ajuste** (a `dataEntrada` mantém a regra; a exclusão do relatório segue pela observação, não pela data); **patrimônio vazio** deixa de bloquear — importa NULO com `ativos.pendencia = 'sem patrimônio físico'`, visível na lista (badge + filtro) e em `/pendencias` (bucket próprio, que acolhe também os 61 "patrimônio não canônico" do go-live), movimentável e corrigível na ficha (que encerra a pendência); erros do mesmo tipo (`patrimonio_invalido`/`sem_data_entrada`/`patrimonio_vazio`) num **card só**, com **sugestão de 1 clique do Hostname** quando ele canonicaliza. Só-números seguem bloqueantes. Migration `0034` (patrimônio nullable + índice único parcial da service tag + RPC com pendência/data-do-ajuste/conferência agregada — `create or replace` puro, 4 args, sem overload). `lint`+`test`(506)+`build` verdes; invariantes F7/F7B intactas (diff da RPC = só as 5 emendas). Decisões e ata do rollout em `docs/DECISOES.md` (2026-07-17 · F7E). **Em produção em 17/07/2026**: `0034` aplicada pelo Johnny no SQL Editor (o classificador bloqueia a DDL destrutiva em produção — mesmo gate da F7/F7B; na 1ª tentativa o editor rodou a `0033` por engano e nada aplicou, ver a ata), conferida antes do merge (`patrimonio` nullable + índice parcial + RPC 1 linha/4 args/grants + as 5 emendas + `v_pendencias` inalterada), merge `78452d9`, deploy Vercel READY, smoke de produção só-leitura OK (RPC de 4 args → 401/42501, `?patrimonio=is.null` → 200; advisors sem novidade)
- [x] **F7F — facilitadores do import antes do próximo go-live** — concluída em 17/07/2026 (execução multi-agente `F7F-ultracode.md`: W1 motor ∥ W2 banco/actions → integração → W3 UI → **W4 revisão adversarial de 5 dimensões + E2E em DEV + emendas**). Cinco defeitos P1–P5, **sem migration** (TypeScript + UI apenas): **(P1)** o "Substituir tudo" deixa de morrer no erro genérico sem diagnóstico — `traduzErroBanco` mapeia por **SQLSTATE + substring** (timeout `57014`, índice parcial `23505`, os `raise` `P0001` da RPC — TOCTOU, plano vazio, termo multi-filial, categoria/estado inválido, identidade repetida, divergência), sempre **logando** `{code,mensagem}` no servidor, e o corpo da Server Action sobe para **8 MB** (`bodySizeLimit`; maior plano real ~0,7 MB). **Sem 0035** — provado em DEV que `SET statement_timeout` dentro da RPC é no-op e que ~1.200 ativos rodam em ~1,0–1,4 s. **(P2)** patrimônio ausente cujo **Hostname** traz um patrimônio canônico embutido (`NB-WAP0001234`) é **auto-preenchido** no preview com o token limpo — aviso **âmbar informativo** `patrimonio_do_hostname`, auditável, fora do fluxo de correção; a reanálise segue juíza (duplicata reaparece; patrimônio com valor inválido nunca é sobrescrito). **(P3)** "Aplicar todas as correções" passa a incluir grupos pontuais **parcialmente preenchidos** (aplica as linhas válidas, banner "N aplicadas · faltam M"). **(P4)** tier **âmbar** (`--warning`, contraste AA em claro e escuro) separa aviso de erro vermelho — patrimônio vazio é âmbar, não vermelho. **(P5)** `try/catch` em `aplicar()`/`analisarCom()` para o throw cru (payload/413/rede) virar toast. Extrator do hostname (`extrairPatrimonioDoHostname`, folha client-safe) é a **mesma régua** do motor e da UI; só captura o token `[A-Z]{2,4}\d{7}` delimitado e nunca interpola/executa payload (testes de injeção). Invariantes F7/F7B/F7E intactas (TOCTOU, advisory lock, backup, confirmação pelo nome, `arquivoHash` do original, RLS); import sem casos novos idêntico ao F7E. `lint`+`tsc`+`test`(539)+`build` verdes. Decisões em `docs/DECISOES.md` (2026-07-17 · F7F)
- [x] **F7G/F7H/F8 — import: `.xlsx` nativo + compra de abertura de volta a baseline** — 20/07/2026. **F7G** (deploy-only, sem migration): `admin/importar` passa a ler o **`.xlsx` nativo** (ExcelJS, MIT, aprovada pelo Johnny) além do CSV — conserta ~355 datas da Matriz que a reexportação CSV do Excel perdia (Inclusão vazia/`#######`, entrega `dd/MMM` sem âncora de ano, acentos `�`); convertido no mesmo `CsvCru`, todo o motor e as salvaguardas reaproveitados. **F7H** (migration 0035): tentou fazer a compra COM data real do import aparecer nas **Entradas** — **revertida pela F8**: a planilha de startup não distingue "compra nova" de "saldo de abertura" (toda linha tem data), então inundava as Entradas com o acervo pré-existente (809 na Matriz). **F8** (`docs/prompts/F8-corrigir-import-compra-abertura.md`, migration `0036`): a **compra de abertura volta a ser SEMPRE baseline** — marcada `import startup dd/MM/yyyy` com **ou sem** data, fora do relatório do período; a **data real segue na ficha / linha do tempo / as-of**. Compra "de verdade" é a lançada manualmente pós-go-live (sem marcador → aparece nas Entradas). Diff `0036` vs `0035` = **só o passo 4b** da RPC (volta ao corpo da `0034`; 4b executável byte-idêntico); relatório sem mudança de código (só comentários + 1 teste que trava `OBS_IMPORT_STARTUP`). **Aplicada e testada no DEV** (smoke: as duas compras — com e sem data — marcadas; 0 nas Entradas). `lint`+`test`+`build` verdes. **Produção — entregue ao Johnny** (gate do classificador barra a DDL da função e o UPDATE em `movimentacoes`): SQL em `scratchpad/f8-sql-producao.sql` — backup + UPDATE que re-marca as **809** compras da Matriz (predicado inverso da F7H, marcador reconstruído byte-exato, backup em `scratchpad/f8-backup-marcador-compras-matriz.json`) + migration `0036` + `notify pgrst`. **NÃO reimportar a Matriz antes da `0036`.** Decisões, contagens (antes/depois) e ata em `docs/DECISOES.md` (2026-07-20 · F8); spec §10.2 (Emenda F8, F7H marcada como superada). **Fora de escopo (decidido):** import de histórico saída/devolução **não será construído** (decisão do Johnny, 20/07/2026) — traria as saídas/devoluções reais como movimentações do período; a entrada do dia a dia segue 100% manual
- [x] **F7-pós — patrimônio ausente/fora-de-formato no import: hostname automático + ausência textual ampliada** — 20/07/2026 (deploy-only, sem migration). Pedidos do Johnny na tela `admin/importar`: (1) o **auto-preenchimento do patrimônio pelo hostname** (F7F) vira **correção automática silenciosa** — sai da contagem/tabela de avisos e da lista de erros, fica só num painel neutro de auditoria (com "Valor original") — **e passa a valer também para patrimônio FORA DE FORMATO** (`12345`/`ABC` com hostname canônico → substituído automaticamente; **revoga** a invariante F7F "inválido nunca é sobrescrito"); (2) **tudo que declare ausência de plaqueta importa vazio** (nulo + pendência), sem mais o bloqueante "fora do formato (ex.: WAP0004491)" — `patrimonioVazio` reconhece as formas produtivas (`sem <algo>`, `s/<algo>`, `n/i`, `não possui/tem/consta…`, `nenhum/nada/inexistente/ausente`, só-símbolos). Prioridade: célula canônica vence → hostname canônico substitui → ausência vira nulo → fora-de-formato **sem** hostname segue bloqueante. **Decisão 5 intacta** (só-números e lixo sem hostname seguem bloqueando; guarda blinda patrimônio válido; cru em `patrimonioOriginal`). (4ª leva) **o campo de patrimônio do card "sem patrimônio" nasce EM BRANCO** — o marcador cru (`n/a`, `SEM PATRIMONIO`…) não é mais pré-preenchido (some o vermelho "ainda fora do formato"; vira o âmbar "opcional — preencha se souber") e não fica no `patrimonio_original` (ficha mostra "—"). Só TS/UI (`deparas.ts`, `plano.ts`, `grupos-erros.tsx`, `importar-wizard.tsx`, `correcoes-aplicadas.tsx`); `lint`+`test`(551)+`build` verdes; nada a rodar em produção além do deploy. Decisões em `docs/DECISOES.md` (2026-07-20 · F7-pós 2ª/3ª/4ª leva) e spec §10.2 (Emenda F7-pós)
- [x] **F7J — import: hostname com patrimônio curto + forçar fora do padrão + limpar patrimônio** — 20/07/2026 (deploy-only + migration `0037`). Três pedidos do Johnny (decididos por 4 perguntas fechadas): **(1)** o reconhecimento do patrimônio no **hostname** aceita prefixo + **<7 dígitos** (`NB-PRO3694`→`PRO0003694`, completa zeros), só para **prefixos conhecidos** (`WAP, PRO, LEA, TEC, STF, PAT, NOO`) — `PC-01`/`SALA-5` não viram patrimônio; **(2)** valor **fora do formato** (`LEA7LYHQH4`, `STF003LOC`) pode ser **forçado** ("Usar mesmo assim") e vira o **patrimônio de verdade** (não-canônico; busca por substring; cru em `patrimonio_original`) — **relaxa a régua canônica**: a validação de formato sai do banco (migration `0037` deixa a RPC só com sanidade não-vazio/≤60) e vira do motor/UI (op `forcar_patrimonio`, auditada); **(3)** botão **"Sem patrimônio"** que **limpa** o patrimônio na correção (vira pendência), o que antes não dava com o campo vazio. Prioridade: célula canônica > forçado > hostname > ausência (nulo) > bloqueante. Diff `0037` vs `0036` = só o passo 1d; **aplicada e testada no DEV** (smoke: `LEA7LYHQH4` importa como patrimônio). `lint`+`test`(561)+`build` verdes. **Produção — entregue ao Johnny** (gate): `scratchpad/f7j-sql-producao.sql` (só a `0037` + `notify pgrst`; **nenhum dado tocado**). Decisões em `docs/DECISOES.md` (2026-07-20 · F7J) e spec §10.2 (Emenda F7J)
- [ ] **F6C — carga dos saldos de itens** (`docs/prompts/F6C-carga-saldos-itens.md`): **por último**, quando o export da planilha de gestão online existir — decisão do Johnny (16/07/2026): melhorias primeiro, cargas depois
- [ ] F5 — refino (alertas, e-mail, estoque mínimo por item; upload do PDF assinado — item 5.5)

Pendências não bloqueantes: perguntas 4, 5, 6 e 7 da spec §13. A nº 1 — filiais oficiais — foi **respondida em 15/07/2026**: Matriz, CD-Afonso Pena, Linhares, Eusébio e Serra (Serra Park é filial própria; Filial-CE = Eusébio).

## Próximo passo

**F6C — carga dos saldos de itens** (`docs/prompts/F6C-carga-saldos-itens.md`) — **por último**, quando o Johnny entregar o export da planilha de gestão online (decisão de 16/07/2026: melhorias primeiro, cargas depois). É o único item que **depende de insumo do Johnny** (o CSV/export dos saldos); a carga marcará os lançamentos de abertura com `observacao = 'saldo inicial (go-live)'`, que a F6B já exclui da tabela de movimentações de itens do relatório. Até lá, o catálogo de `itens` segue vazio e essa seção do relatório só aparece quando houver lançamentos. O backlog aberto está em `docs/DECISOES.md` (F6B — atalho `?` da Ajuda, import da constante `OBS_SALDO_INICIAL` na carga) e o F5 de refino (alertas, e-mail, estoque mínimo por item, upload do PDF assinado — item 5.5).

As ordens rodam em **modo autônomo com acesso total** (decisão de 09/07/2026, regras no `CLAUDE.md`): o Claude executa tudo — decisões, merge, deploy e produção — sem pedir autorização, compensando com autoproteções (backup/dry-run em operação destrutiva) e rastro auditável em `docs/DECISOES.md`.

Importante: o sistema **não nasce com os dados reais**. O desenvolvimento roda com dados fictícios; os dados reais entram por **carga de go-live**: a carga global inicial pelos **scripts da F4** (autônoma, na janela do go-live) e, desde a **F7**, o **import de startup por filial** pela tela `admin/importar` (só *Substituir tudo*, go-live novo de uma filial — spec §10.2; os erros do CSV se corrigem na própria tela desde a F7B). A entrada de dados **do dia a dia continua 100% manual** pelo sistema — não há sincronização recorrente nem modo *Atualizar* —, e ser mais prático que o Excel é o requisito central (facilitadores da F2).
